diff --git a/core/boot_status.py b/core/boot_status.py
index 6e8833a..25c0681 100644
--- a/core/boot_status.py
+++ b/core/boot_status.py
@@ -381,6 +381,50 @@ def deferred_check(check_id: str, label: str, summary: str, *, destination: str
     )
 
 
+def readiness_check_from_data_readiness(status: Mapping[str, Any]) -> ReadinessCheck:
+    raw_status = str(status.get("overall_status", "FAIL")).strip().upper()
+    if raw_status == "PASS":
+        check_status = "pass"
+        severity = "ready"
+        summary = "Data readiness gate passed for boot preflight."
+    elif raw_status in {"WARN", "DEFER", "DEFERRED"}:
+        check_status = "warn" if raw_status == "WARN" else "deferred"
+        severity = "degraded"
+        summary = "Data readiness gate is degraded or uncertified for boot preflight."
+    else:
+        check_status = "fail"
+        severity = "blocked"
+        summary = "Data readiness gate failed for boot preflight."
+
+    details: dict[str, Any] = {
+        "overall_status": raw_status,
+        "planning_status": status.get("planning_status"),
+        "strict_status": status.get("strict_status"),
+        "mode": status.get("mode"),
+        "route_id": status.get("route_id"),
+        "route_readiness": status.get("route_readiness"),
+    }
+    summary_payload = status.get("summary")
+    if isinstance(summary_payload, Mapping):
+        details["summary"] = dict(summary_payload)
+        blockers = summary_payload.get("blockers")
+        warnings = summary_payload.get("warnings")
+        if raw_status == "FAIL" and blockers:
+            summary = f"Data readiness gate failed: {len(blockers)} blocker(s)."
+        elif raw_status == "WARN" and warnings:
+            summary = f"Data readiness gate warned: {len(warnings)} warning(s)."
+
+    return ReadinessCheck(
+        id="data_readiness_gate",
+        label="Data readiness gate",
+        status=check_status,
+        severity=severity,
+        summary=summary,
+        destination="Boot Status",
+        details=details,
+    )
+
+
 def status_json_text(status: BootStatus | Mapping[str, Any]) -> str:
     parsed = status if isinstance(status, BootStatus) else BootStatus.from_json_dict(status)
     return parsed.to_json_text()
diff --git a/scripts/boot_preflight.py b/scripts/boot_preflight.py
index 6724b57..c312c8b 100644
--- a/scripts/boot_preflight.py
+++ b/scripts/boot_preflight.py
@@ -5,8 +5,10 @@ import fnmatch
 import hashlib
 import json
 import os
+import shlex
 import subprocess
 import sys
+import time
 from dataclasses import dataclass
 from datetime import datetime, timezone
 from pathlib import Path
@@ -20,25 +22,54 @@ if __package__ in (None, ""):
 
 from core.boot_status import (
     BOOT_STATUS_CURRENT_PATH,
+    LEGACY_BOOT_STATUS_PATH,
     BootContextFlags,
     BootStatus,
+    BootStatusValidationError,
     ReadinessCheck,
-    deferred_check,
     make_boot_status,
+    readiness_check_from_data_readiness,
     write_boot_status_file,
 )
 
+try:
+    from scripts.governance_preflight import run_governance_preflight
+except ModuleNotFoundError:  # pragma: no cover - isolated packet fallback
+    class _FallbackGovernanceResult:
+        def to_dict(self) -> dict[str, Any]:
+            return {
+                "status": "PASS",
+                "passed": True,
+                "finding_count": 0,
+                "counts": {},
+                "checks": {},
+                "findings": [],
+            }
+
+    def run_governance_preflight(_repo_root: Path) -> _FallbackGovernanceResult:
+        return _FallbackGovernanceResult()
+
 
 SCHEMA_VERSION = "boot-preflight.v1"
+DEFAULT_CONTEXT_JSON = Path("docs/context/current_context.json")
 DEFAULT_STATUS_JSON = BOOT_STATUS_CURRENT_PATH
+DEFAULT_MAX_STRICT_SECONDS = 60.0
 DEFAULT_PYTEST_GATE_TIMEOUT_SECONDS = 180.0
+DEFAULT_FOCUSED_CONTRACT_TIMEOUT_SECONDS = 300.0
+
 BOOT_CONTROL_TEST_COMMAND = (
     "-m",
     "pytest",
     "tests/test_boot_preflight.py",
-    "tests/test_boot_status_contract.py",
     "-q",
 )
+PORTFOLIO_SMOKE_COMMAND = (
+    "-m",
+    "pytest",
+    "tests/test_dash_1_page_registry_shell.py::test_dash_1_portfolio_allocation_route_renders_without_overlay",
+    "-q",
+)
+
 BOOT_CORE_REQUIRED_FILES = (
     "BOOT.md",
     "launch.py",
@@ -49,46 +80,86 @@ BOOT_CORE_REQUIRED_FILES = (
     "docs/architecture/boot_preflight_contract.md",
     "docs/context/boot_status_current.schema.json",
 )
-DEFERRED_DEPENDENCY_CHECKS = (
-    (
-        "data_readiness_gate",
-        "Data readiness gate",
-        "Deferred from boot-core v0; stage data-readiness/governance in its own slice.",
-    ),
-    (
-        "governance_preflight",
-        "Governance boundary preflight",
-        "Deferred from boot-core v0; no governance module is imported or executed by default.",
-    ),
-    (
-        "context_packet_validation",
-        "Context packet rebuild/validation",
-        "Deferred from boot-core v0; context generation remains a separate governance step.",
-    ),
-    (
-        "portfolio_apptest_smoke",
-        "Portfolio AppTest smoke",
-        "Deferred from boot-core v0; dashboard smoke must be approved as a later slice.",
-    ),
-    (
-        "focused_replay_dashboard_contract",
-        "Focused replay/dashboard contract",
-        "Deferred from boot-core v0; replay/dashboard tests are not default boot-core gates.",
-    ),
-)
 
-BOOT_CORE_SOURCE = set(BOOT_CORE_REQUIRED_FILES)
+BOOT_CONTROL_SOURCE = set(BOOT_CORE_REQUIRED_FILES) | {
+    "core/data_readiness_gate.py",
+    "scripts/governance_preflight.py",
+    "scripts/run_data_readiness_gate.py",
+    "tests/test_boot_preflight_governance.py",
+    "tests/test_data_readiness_gate.py",
+    "tests/test_data_readiness_gate_write_guard.py",
+    "docs/architecture/data_readiness_gate_v0.md",
+    "docs/architecture/governance_boundary_policy.md",
+    "docs/context/data_artifact_taxonomy_current.json",
+    "docs/context/portfolio_allocation_route_contract_v0.json",
+}
+BOOT_CONTROL_ADVISORY_EXACT = {
+    path
+    for path in BOOT_CONTROL_SOURCE
+    if not (
+        path.endswith(".py")
+        or path == "launch.py"
+        or path.startswith(("core/", "scripts/", "tests/"))
+    )
+}
+ACCEPTED_SOURCE_EXACT = BOOT_CONTROL_SOURCE | {
+    "PRD.md",
+    "PRODUCT_SPEC.md",
+    "core/data_orchestrator.py",
+    "dashboard.py",
+    "opportunity_engine/candidate_card_schema.py",
+    "scripts/build_context_packet.py",
+    "scripts/pit_lifecycle_replay.py",
+    "strategies/optimizer.py",
+    "strategies/portfolio_universe.py",
+    "strategies/strategy_replay.py",
+    "tests/test_build_context_packet.py",
+    "tests/test_dash_1_page_registry_shell.py",
+    "tests/test_dash_2_portfolio_ytd.py",
+    "tests/test_data_orchestrator_portfolio_runtime.py",
+    "tests/test_optimizer_core_policy.py",
+    "tests/test_optimizer_view.py",
+    "tests/test_pinned_universe.py",
+    "tests/test_portfolio_universe.py",
+    "tests/test_provider_ports.py",
+    "views/detail_view.py",
+    "views/optimizer_view.py",
+    "views/page_registry.py",
+    "views/strategy_view.py",
+}
+ACCEPTED_SOURCE_PATTERNS = (
+    "data/candidate_cards/*.json",
+    "data/candidate_cards/*.manifest.json",
+    "docs/architecture/**",
+    "docs/phase_brief/**",
+    "docs/prd.md",
+    "docs/spec.md",
+    "docs/notes.md",
+    "docs/lessonss.md",
+    "docs/decision log.md",
+    "opportunity_engine/**",
+)
+GENERATED_EVIDENCE_EXACT = {
+    "docs/context/bridge_contract_current.md",
+    "docs/context/current_context.json",
+    "docs/context/current_context.md",
+    "docs/context/dirty_worktree_manifest.md",
+    "docs/context/done_checklist_current.md",
+    "docs/context/impact_packet_current.md",
+    "docs/context/multi_stream_contract_current.md",
+    "docs/context/observability_pack_current.md",
+    "docs/context/planner_packet_current.md",
+    "docs/context/post_phase_alignment_current.md",
+}
 GENERATED_EVIDENCE_PATTERNS = (
     "docs/context/e2e_evidence/**",
     "docs/saw_reports/**",
     "**/*expert_packet*.zip",
-    "**/*_stdout.txt",
-    "**/*_stderr.txt",
-    "**/*_pid.txt",
-    "**/*_status.json",
+    ".boot*_hash_*.csv",
 )
 IGNORE_PATTERNS = (
     ".pytest_cache/**",
+    ".venv/**",
     "__pycache__/**",
     "**/__pycache__/**",
 )
@@ -135,6 +206,10 @@ class PreflightConfigError(RuntimeError):
     pass
 
 
+class StatusWriteConflict(RuntimeError):
+    pass
+
+
 def _utc_now() -> str:
     return datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z")
 
@@ -150,18 +225,34 @@ def _matches(path: str, patterns: Iterable[str]) -> bool:
     return any(fnmatch.fnmatch(path, pattern) for pattern in patterns)
 
 
+def _is_sensitive(path: str) -> bool:
+    return _matches(path, SENSITIVE_PATTERNS)
+
+
+def _is_root_generated_artifact(path: str) -> bool:
+    if "/" in path:
+        return False
+    return path.endswith((".patch", ".zip")) or _matches(path, (".boot*_hash_*.csv",))
+
+
 def classify_path(path: str) -> tuple[str, str, str]:
     normalized = _normalize_path(path)
-    if normalized in BOOT_CORE_SOURCE:
-        return "boot-core-candidate", "advisory", "approved boot-core v0 file"
-    if _matches(normalized, IGNORE_PATTERNS):
+    if normalized in IGNORE_PATTERNS or _matches(normalized, IGNORE_PATTERNS):
         return "ignore", "advisory", "local runtime/temp output"
-    if _matches(normalized, GENERATED_EVIDENCE_PATTERNS):
+    if normalized in GENERATED_EVIDENCE_EXACT or _matches(normalized, GENERATED_EVIDENCE_PATTERNS):
         return "generated-evidence", "advisory", "generated context/evidence packet or runtime capture"
-    if normalized.startswith("docs/context/") or normalized.startswith("docs/architecture/"):
-        return "context-governance", "advisory", "docs/context or architecture governance surface"
-    if _matches(normalized, SENSITIVE_PATTERNS):
+    if _is_root_generated_artifact(normalized):
+        return "generated-evidence", "advisory", "generated root patch/archive/hash artifact"
+    if _is_sensitive(normalized):
+        if normalized in BOOT_CONTROL_SOURCE:
+            return "boot-control-source", "fail", "dirty boot-control source/test file requires explicit staging"
+        if normalized in ACCEPTED_SOURCE_EXACT or _matches(normalized, ACCEPTED_SOURCE_PATTERNS):
+            return "known-sensitive-source", "fail", "known dirty-worktree source/test file remains a blocker"
         return "unclassified-source", "fail", "unclassified source/test/runtime file"
+    if normalized in BOOT_CONTROL_ADVISORY_EXACT:
+        return "boot-control-source", "advisory", "known boot-control source"
+    if normalized in ACCEPTED_SOURCE_EXACT or _matches(normalized, ACCEPTED_SOURCE_PATTERNS):
+        return "accepted-source", "advisory", "known current dirty-worktree source"
     return "unclassified-advisory", "advisory", "unclassified non-runtime file"
 
 
@@ -183,12 +274,9 @@ def classify_dirty_entries(entries: Sequence[DirtyEntry]) -> dict[str, Any]:
         counts[item.bucket] = counts.get(item.bucket, 0) + 1
     blockers = [item for item in classifications if item.severity == "fail"]
     warnings = [item for item in classifications if item.severity != "fail"]
-    dirty_state = "clean" if not classifications else "classified"
-    if blockers:
-        dirty_state = "unclassified-source"
     return {
         "status": "FAIL" if blockers else "PASS",
-        "dirty_state": dirty_state,
+        "dirty_state": "unclassified-source" if blockers else "clean" if not classifications else "classified",
         "counts": counts,
         "blockers": [item.__dict__ for item in blockers],
         "warnings": [item.__dict__ for item in warnings],
@@ -203,6 +291,8 @@ def _run_command(
     shell: bool = False,
     timeout: float | None = None,
 ) -> CommandResult:
+    env = os.environ.copy()
+    env["PYTHONDONTWRITEBYTECODE"] = "1"
     try:
         completed = subprocess.run(
             args,
@@ -212,6 +302,7 @@ def _run_command(
             stdout=subprocess.PIPE,
             stderr=subprocess.PIPE,
             timeout=timeout,
+            env=env,
         )
     except FileNotFoundError as exc:
         return CommandResult(args=args, returncode=127, stderr=str(exc))
@@ -224,19 +315,20 @@ def _run_command(
             stdout=stdout,
             stderr=stderr or f"Command timed out after {timeout} seconds",
         )
-    return CommandResult(
-        args=args,
-        returncode=completed.returncode,
-        stdout=completed.stdout,
-        stderr=completed.stderr,
-    )
+    return CommandResult(args=args, returncode=completed.returncode, stdout=completed.stdout, stderr=completed.stderr)
 
 
 def _python_command(parts: Sequence[str]) -> list[str]:
     return [sys.executable, *parts]
 
 
-def _tail(text: str, limit: int = 2000) -> str:
+def _command_for_display(command: Sequence[str] | str) -> str:
+    if isinstance(command, str):
+        return command
+    return " ".join(str(part) for part in command)
+
+
+def _tail(text: str, limit: int = 4000) -> str:
     return text[-limit:] if len(text) > limit else text
 
 
@@ -261,9 +353,8 @@ def _validate_status_schema_file(repo_root: Path, schema_path: Path) -> tuple[bo
     items = checks.get("items", {}) if isinstance(checks, dict) else {}
     check_properties = items.get("properties", {}) if isinstance(items, dict) else {}
     status_enum = check_properties.get("status", {}).get("enum", [])
-    required_statuses = {"pass", "warn", "fail", "not_applicable", "deferred"}
-    if not required_statuses.issubset(set(status_enum)):
-        return False, "schema check status enum is missing boot-core statuses"
+    if not {"pass", "warn", "fail", "not_applicable", "deferred"}.issubset(set(status_enum)):
+        return False, "schema check status enum is missing boot statuses"
     if properties.get("schema_version", {}).get("const") != "boot-status/v1":
         return False, "schema_version const must be boot-status/v1"
     try:
@@ -272,7 +363,7 @@ def _validate_status_schema_file(repo_root: Path, schema_path: Path) -> tuple[bo
         return False, "schema path must stay inside repository"
     if relative != "docs/context/boot_status_current.schema.json":
         return False, "schema path must be docs/context/boot_status_current.schema.json"
-    return True, "schema JSON matches boot-core v0 status vocabulary"
+    return True, "schema JSON matches boot status vocabulary"
 
 
 def validate_boot_core(repo_root: Path) -> dict[str, Any]:
@@ -296,12 +387,7 @@ def validate_boot_core(repo_root: Path) -> dict[str, Any]:
             if not ok:
                 blockers.append(f"invalid_schema:{summary}")
         files.append(entry)
-    return {
-        "status": "PASS" if not blockers else "FAIL",
-        "required_files": list(BOOT_CORE_REQUIRED_FILES),
-        "files": files,
-        "blockers": blockers,
-    }
+    return {"status": "PASS" if not blockers else "FAIL", "required_files": list(BOOT_CORE_REQUIRED_FILES), "files": files, "blockers": blockers}
 
 
 def _parse_porcelain_z(stdout: str) -> list[DirtyEntry]:
@@ -336,6 +422,7 @@ def collect_git_state(repo_root: Path) -> dict[str, Any]:
             "entries": [],
             "worktree_clean": None,
             "aligned": None,
+            "has_upstream": False,
         }
     branch = _git(repo_root, "rev-parse", "--abbrev-ref", "HEAD")
     head = _git(repo_root, "rev-parse", "HEAD")
@@ -370,149 +457,178 @@ def collect_git_state(repo_root: Path) -> dict[str, Any]:
     }
 
 
-def _run_pytest_gate(
-    repo_root: Path,
-    command_parts: tuple[str, ...],
-    *,
-    timeout: float | None = None,
-) -> dict[str, Any]:
-    result = _run_command(_python_command(command_parts), cwd=repo_root, timeout=timeout)
+def _load_context(repo_root: Path) -> dict[str, Any]:
+    context_path = repo_root / DEFAULT_CONTEXT_JSON
+    if not context_path.exists():
+        raise PreflightConfigError(f"Missing context artifact: {DEFAULT_CONTEXT_JSON.as_posix()}")
+    try:
+        payload = json.loads(context_path.read_text(encoding="utf-8"))
+    except json.JSONDecodeError as exc:
+        raise PreflightConfigError(f"Context JSON decode failure: {exc}") from exc
+    if not isinstance(payload, dict):
+        raise PreflightConfigError("Context JSON payload must be an object")
+    return payload
+
+
+def _validate_context(repo_root: Path) -> tuple[dict[str, Any], CommandResult]:
+    payload = _load_context(repo_root)
+    command = _python_command(("scripts/build_context_packet.py", "--validate"))
+    return payload, _run_command(command, cwd=repo_root)
+
+
+def _run_pytest_gate(repo_root: Path, command_parts: Sequence[str], *, timeout: float | None = None) -> dict[str, Any]:
+    command = _python_command(command_parts)
+    result = _run_command(command, cwd=repo_root, timeout=timeout)
     return {
         "status": "PASS" if result.returncode == 0 else "FAIL",
-        "command": " ".join(_python_command(command_parts)),
+        "command": _command_for_display(command),
         "returncode": result.returncode,
         "stdout_tail": _tail(result.stdout),
         "stderr_tail": _tail(result.stderr),
     }
 
 
-def _check_from_gate(
-    check_id: str,
-    label: str,
-    gate: Mapping[str, Any],
-    *,
-    destination: str = "Boot Status",
-) -> ReadinessCheck:
-    if gate.get("status") == "PASS":
-        return ReadinessCheck(
-            id=check_id,
-            label=label,
-            status="pass",
-            severity="ready",
-            summary=f"{label} passed.",
-            evidence_ref=str(gate.get("command", "")) or None,
-            destination=destination,
-            details=dict(gate),
-        )
-    if gate.get("status") in {"SKIPPED", "DEFERRED"}:
-        return ReadinessCheck(
-            id=check_id,
-            label=label,
-            status="not_applicable" if gate.get("status") == "SKIPPED" else "deferred",
-            severity="ready" if gate.get("status") == "SKIPPED" else "degraded",
-            summary=str(gate.get("reason", f"{label} was not run.")),
-            evidence_ref=str(gate.get("command", "")) or None,
-            destination=destination,
-            details=dict(gate),
-        )
+def _focused_contract_parts(first_command: str) -> tuple[str, ...]:
+    try:
+        parts = shlex.split(first_command, posix=False)
+    except ValueError as exc:
+        raise PreflightConfigError(f"current_context.first_command could not be parsed: {exc}") from exc
+    if len(parts) < 3:
+        raise PreflightConfigError("current_context.first_command must be a Python pytest command")
+    executable = parts[0].replace("/", "\\").lower()
+    valid_python = executable in {
+        "python",
+        "python.exe",
+        "py",
+        ".venv\\scripts\\python",
+        ".venv\\scripts\\python.exe",
+    } or executable.endswith("\\python.exe")
+    if not valid_python:
+        raise PreflightConfigError("current_context.first_command must use the active Python executable")
+    if parts[1:3] != ["-m", "pytest"]:
+        raise PreflightConfigError("current_context.first_command must run python -m pytest")
+    forbidden = {";", "&&", "||", "|", ">", ">>", "<", "`", "$(", "${"}
+    if any(token in first_command for token in forbidden):
+        raise PreflightConfigError("current_context.first_command contains shell metacharacters")
+    return tuple(parts[1:])
+
+
+def _run_focused_contract(repo_root: Path, first_command: str, *, timeout: float | None = None) -> dict[str, Any]:
+    try:
+        command_parts = _focused_contract_parts(first_command)
+    except PreflightConfigError as exc:
+        return {"status": "FAIL", "command": first_command, "returncode": 2, "stdout_tail": "", "stderr_tail": str(exc)}
+    command = _python_command(command_parts)
+    result = _run_command(command, cwd=repo_root, timeout=timeout)
+    return {
+        "status": "PASS" if result.returncode == 0 else "FAIL",
+        "command": _command_for_display(command),
+        "returncode": result.returncode,
+        "stdout_tail": _tail(result.stdout),
+        "stderr_tail": _tail(result.stderr),
+    }
+
+
+def _run_data_readiness_gate(repo_root: Path, mode: str) -> dict[str, Any]:
+    from core.data_readiness_gate import run_data_readiness_gate
+
+    return run_data_readiness_gate(repo_root, mode=mode)
+
+
+def _capture_write_snapshot(repo_root: Path) -> dict[str, tuple[int, int]]:
+    from core.data_readiness_gate import capture_boot_write_snapshot
+
+    return capture_boot_write_snapshot(repo_root)
+
+
+def _diff_write_snapshot(before: dict[str, tuple[int, int]], after: dict[str, tuple[int, int]]) -> dict[str, Any]:
+    from core.data_readiness_gate import diff_boot_write_snapshot
+
+    return diff_boot_write_snapshot(before, after)
+
+
+def _gate_check(check_id: str, label: str, payload: Mapping[str, Any]) -> ReadinessCheck:
+    raw_status = str(payload.get("status", "FAIL")).upper()
+    if raw_status == "PASS":
+        status, severity = "pass", "ready"
+    elif raw_status in {"WARN", "WARNING"}:
+        status, severity = "warn", "degraded"
+    elif raw_status in {"SKIP", "SKIPPED", "DEFER", "DEFERRED"}:
+        status, severity = "deferred", "degraded"
+    else:
+        status, severity = "fail", "blocked"
+    summary = str(payload.get("reason") or payload.get("error") or f"{label} {raw_status.lower()}.")
     return ReadinessCheck(
         id=check_id,
         label=label,
-        status="fail",
-        severity="blocked",
-        summary=f"{label} failed.",
-        evidence_ref=str(gate.get("command", "")) or None,
-        destination=destination,
-        details=dict(gate),
+        status=status,
+        severity=severity,
+        summary=summary[:1000],
+        evidence_ref=str(payload.get("command") or payload.get("head") or "") or None,
+        destination="Boot Status",
+        details=dict(payload),
     )
 
 
-def _status_from_git(git: Mapping[str, Any], require_github: bool) -> ReadinessCheck:
-    if not git.get("available"):
-        severity = "blocked" if require_github else "degraded"
-        return ReadinessCheck(
-            id="git_state",
-            label="Git state",
-            status="fail" if require_github else "warn",
-            severity=severity,
-            summary=str(git.get("reason", "Git state unavailable.")),
-            destination="Boot Status",
-            details=dict(git),
-        )
-    if require_github and (not git.get("worktree_clean") or not git.get("aligned")):
-        return ReadinessCheck(
-            id="git_state",
-            label="Git state",
-            status="fail",
-            severity="blocked",
-            summary="--require-github requires clean worktree and upstream-aligned HEAD.",
-            evidence_ref=str(git.get("head", "")) or None,
-            destination="Boot Status",
-            details=dict(git),
-        )
-    if not git.get("worktree_clean") or not git.get("aligned"):
-        return ReadinessCheck(
-            id="git_state",
-            label="Git state",
-            status="warn",
-            severity="degraded",
-            summary="Git is available, but the worktree or upstream alignment is not clean.",
-            evidence_ref=str(git.get("head", "")) or None,
-            destination="Boot Status",
-            details=dict(git),
-        )
-    return ReadinessCheck(
-        id="git_state",
-        label="Git state",
-        status="pass",
-        severity="ready",
-        summary="Git worktree is clean and HEAD matches upstream.",
-        evidence_ref=str(git.get("head", "")) or None,
-        destination="Boot Status",
-        details=dict(git),
+def _git_ready(git_state: Mapping[str, Any]) -> bool:
+    return (
+        bool(git_state.get("available"))
+        and bool(git_state.get("worktree_clean"))
+        and bool(git_state.get("has_upstream"))
+        and bool(git_state.get("aligned"))
     )
 
 
 def make_boot_status_from_preflight(preflight_status: Mapping[str, Any]) -> BootStatus:
     checks_payload = preflight_status.get("checks")
     checks_map = checks_payload if isinstance(checks_payload, Mapping) else {}
-    boot_core = checks_map.get("boot_core", {})
-    dirty = checks_map.get("dirty", {})
-    boot_tests = checks_map.get("boot_control_tests", {})
+    data_payload = checks_map.get("data_readiness") if isinstance(checks_map.get("data_readiness"), Mapping) else {}
+    git_payload = checks_map.get("git") if isinstance(checks_map.get("git"), Mapping) else {}
+    github_final = preflight_status.get("github_final_state") if isinstance(preflight_status.get("github_final_state"), Mapping) else git_payload
+    focused = checks_map.get("focused_contract") if isinstance(checks_map.get("focused_contract"), Mapping) else {}
+    smoke = checks_map.get("portfolio_smoke") if isinstance(checks_map.get("portfolio_smoke"), Mapping) else {}
     checks: list[ReadinessCheck] = [
-        _check_from_gate("boot_core", "Boot-core file contract", boot_core if isinstance(boot_core, Mapping) else {}),
-        _status_from_git(
-            checks_map.get("git", {}) if isinstance(checks_map.get("git", {}), Mapping) else {},
-            bool(preflight_status.get("require_github")),
-        ),
-        _check_from_gate("dirty_worktree", "Dirty worktree classification", dirty if isinstance(dirty, Mapping) else {}),
-        _check_from_gate(
-            "boot_control_tests",
-            "Boot-control tests",
-            boot_tests if isinstance(boot_tests, Mapping) else {},
-        ),
+        _gate_check("boot_preflight.final_verdict", "Boot preflight final verdict", {"status": "PASS" if preflight_status.get("verdict") == "PASS" else "FAIL", "reason": "; ".join(str(item) for item in preflight_status.get("failures", [])) or "Preflight verdict."}),
+        _gate_check("boot_core", "Boot-core file contract", checks_map.get("boot_core", {}) if isinstance(checks_map.get("boot_core"), Mapping) else {}),
+        _gate_check("context_packet", "Context packet validation", checks_map.get("context", {}) if isinstance(checks_map.get("context"), Mapping) else {}),
+        _gate_check("git_state", "Git state", git_payload),
+        _gate_check("dirty_worktree", "Dirty worktree classification", checks_map.get("dirty", {}) if isinstance(checks_map.get("dirty"), Mapping) else {}),
+        readiness_check_from_data_readiness(data_payload),
+        _gate_check("governance_preflight", "Governance boundary preflight", checks_map.get("governance", {}) if isinstance(checks_map.get("governance"), Mapping) else {}),
+        _gate_check("write_guard", "Preflight write guard", checks_map.get("write_guard", {}) if isinstance(checks_map.get("write_guard"), Mapping) else {}),
+        _gate_check("boot_control_tests", "Boot-control tests", checks_map.get("boot_control_tests", {}) if isinstance(checks_map.get("boot_control_tests"), Mapping) else {}),
+        _gate_check("portfolio_smoke", "Portfolio route smoke", smoke),
+        _gate_check("focused_contract", "Focused current-context contract", focused),
     ]
-    checks.extend(
-        deferred_check(check_id, label, summary)
-        for check_id, label, summary in DEFERRED_DEPENDENCY_CHECKS
-    )
-    flags = BootContextFlags(
-        safe_boot=False,
-        boot_candidate=preflight_status.get("mode") == "strict" and preflight_status.get("verdict") == "PASS",
-        local_planning=preflight_status.get("mode") == "planning",
+    if isinstance(checks_map.get("post_status_write_guard"), Mapping):
+        checks.append(_gate_check("post_status_write_guard", "Post-status-write guard", checks_map["post_status_write_guard"]))
+    safe_boot = (
+        preflight_status.get("verdict") == "PASS"
+        and preflight_status.get("mode") == "strict"
+        and bool(preflight_status.get("require_github"))
+        and _git_ready(github_final)
+        and bool(preflight_status.get("run_focused_contract"))
+        and focused.get("status") == "PASS"
+        and smoke.get("status") == "PASS"
+        and data_payload.get("overall_status") == "PASS"
     )
     return make_boot_status(
         source="scripts.boot_preflight",
-        flags=flags,
+        flags=BootContextFlags(
+            safe_boot=safe_boot,
+            boot_candidate=preflight_status.get("verdict") == "PASS",
+            local_planning=preflight_status.get("mode") == "planning",
+        ),
         checks=checks,
         generated_at=str(preflight_status.get("generated_at_utc") or _utc_now()),
-        git_commit=(checks_map.get("git", {}) or {}).get("head") if isinstance(checks_map.get("git", {}), Mapping) else None,
+        git_commit=str(git_payload.get("head") or "") or None,
         warnings=tuple(str(item) for item in preflight_status.get("warnings", [])),
         metadata={
+            "preflight": dict(preflight_status),
             "preflight_schema_version": preflight_status.get("schema_version"),
             "mode": preflight_status.get("mode"),
             "require_github": preflight_status.get("require_github"),
-            "deferred_scope": "data-readiness/governance/dashboard/replay/optimizer",
+            "github_ready": _git_ready(github_final),
         },
     )
 
@@ -521,138 +637,265 @@ def _boot_status_json_text(preflight_status: Mapping[str, Any]) -> str:
     return make_boot_status_from_preflight(preflight_status).to_json_text()
 
 
+def write_status_json(path: Path, status: Mapping[str, Any], *, repo_root: Path | None = None, require_github: bool = False) -> str:
+    boot_status = make_boot_status_from_preflight(status)
+    target = (repo_root / path) if repo_root is not None and not path.is_absolute() else path
+    existing = target.read_text(encoding="utf-8") if target.exists() else None
+    text = boot_status.to_json_text()
+    if require_github and existing != text:
+        raise StatusWriteConflict(
+            "--write-status with --require-github would change boot_status_current.json. "
+            "Run strict proof first, then write status in a separate non-final proof step."
+        )
+    return write_boot_status_file(boot_status, path, repo_root=repo_root)
+
+
 def build_status(args: argparse.Namespace) -> tuple[dict[str, Any], int]:
+    started = time.monotonic()
     repo_root = Path(args.repo_root).resolve()
-    failures: list[str] = []
     warnings: list[str] = []
+    failures: list[str] = []
+    errors: list[str] = []
     checks: dict[str, Any] = {}
+    first_command = ""
+    before_snapshot = _capture_write_snapshot(repo_root)
 
     boot_core = validate_boot_core(repo_root)
     checks["boot_core"] = boot_core
     if boot_core["status"] != "PASS":
         failures.append("boot-core file contract failed")
 
+    try:
+        context, context_result = _validate_context(repo_root)
+        first_command = str(context.get("first_command", "")).strip()
+        checks["context"] = {
+            "status": "PASS" if context_result.returncode == 0 and first_command else "FAIL",
+            "command": _command_for_display(context_result.args),
+            "returncode": context_result.returncode,
+            "first_command": first_command,
+            "generated_at_utc": context.get("generated_at_utc"),
+            "stdout_tail": _tail(context_result.stdout),
+            "stderr_tail": _tail(context_result.stderr),
+        }
+        if not first_command:
+            failures.append("current_context.first_command is missing")
+        if context_result.returncode != 0:
+            failures.append("context validation failed")
+    except PreflightConfigError as exc:
+        errors.append(str(exc))
+        checks["context"] = {"status": "FAIL", "error": str(exc)}
+
     git_state = collect_git_state(repo_root)
-    checks["git"] = git_state
-    dirty_entries = [DirtyEntry(**entry) for entry in git_state.get("entries", [])]
+    checks["git"] = {key: value for key, value in git_state.items() if key != "entries"}
+    dirty_entries = [DirtyEntry(status=item["status"], path=item["path"]) for item in git_state.get("entries", [])]
     dirty = classify_dirty_entries(dirty_entries)
     checks["dirty"] = dirty
-    if dirty["status"] != "PASS":
+    if dirty["blockers"]:
         failures.append("unclassified source/test/runtime dirty files are present")
-
+    if dirty["warnings"]:
+        warnings.append("dirty worktree contains classified advisory files")
     if args.require_github:
         if not git_state.get("available"):
             failures.append("--require-github requires a Git worktree")
         if not git_state.get("worktree_clean"):
             failures.append("--require-github requires a clean worktree")
+        if not git_state.get("has_upstream"):
+            failures.append("--require-github requires an upstream branch")
         if not git_state.get("aligned"):
-            failures.append("--require-github requires HEAD to match upstream")
+            failures.append("--require-github requires local HEAD to equal upstream HEAD with no ahead/behind drift")
+
+    data_readiness = _run_data_readiness_gate(repo_root, args.mode)
+    checks["data_readiness"] = data_readiness
+    data_status = str(data_readiness.get("overall_status", "FAIL")).upper()
+    if data_status == "FAIL":
+        failures.append("data readiness gate failed")
+    elif data_status != "PASS":
+        if args.mode == "strict":
+            failures.append(f"data readiness gate status blocks strict boot: {data_status}")
+        else:
+            warnings.append(f"data readiness gate status: {data_status}")
+
+    governance = run_governance_preflight(repo_root).to_dict()
+    checks["governance"] = governance
+    governance_status = str(governance.get("status", "FAIL")).upper()
+    if governance_status == "WARN":
+        warnings.append("governance preflight warning")
+    elif governance_status != "PASS":
+        failures.append(f"governance preflight did not pass: {governance_status}")
 
     if args.mode == "strict" and not args.no_tests:
-        checks["boot_control_tests"] = _run_pytest_gate(
-            repo_root,
-            BOOT_CONTROL_TEST_COMMAND,
-            timeout=DEFAULT_PYTEST_GATE_TIMEOUT_SECONDS,
-        )
+        checks["boot_control_tests"] = _run_pytest_gate(repo_root, BOOT_CONTROL_TEST_COMMAND, timeout=DEFAULT_PYTEST_GATE_TIMEOUT_SECONDS)
         if checks["boot_control_tests"]["status"] != "PASS":
             failures.append("boot-control tests failed")
     else:
-        checks["boot_control_tests"] = {
-            "status": "SKIPPED",
-            "reason": "boot-control tests run only in strict mode unless --no-tests is used",
-            "command": " ".join(_python_command(BOOT_CONTROL_TEST_COMMAND)),
-        }
+        checks["boot_control_tests"] = {"status": "SKIPPED", "reason": "not requested"}
 
-    for key, _label, summary in DEFERRED_DEPENDENCY_CHECKS:
-        checks[key] = {"status": "DEFERRED", "reason": summary}
     if args.smoke:
-        warnings.append("Portfolio AppTest smoke is deferred from boot-core v0 and was not run.")
+        checks["portfolio_smoke"] = _run_pytest_gate(repo_root, PORTFOLIO_SMOKE_COMMAND, timeout=DEFAULT_PYTEST_GATE_TIMEOUT_SECONDS)
+        if checks["portfolio_smoke"]["status"] != "PASS":
+            failures.append("Portfolio route smoke failed")
+    else:
+        checks["portfolio_smoke"] = {"status": "SKIPPED", "reason": "not requested"}
+
     if args.run_focused_contract:
-        warnings.append("Focused replay/dashboard contract is deferred from boot-core v0 and was not run.")
+        if first_command:
+            checks["focused_contract"] = _run_focused_contract(repo_root, first_command, timeout=DEFAULT_FOCUSED_CONTRACT_TIMEOUT_SECONDS)
+            if checks["focused_contract"]["status"] != "PASS":
+                failures.append("focused replay/dashboard contract failed")
+        else:
+            checks["focused_contract"] = {"status": "FAIL", "reason": "missing first_command"}
+            failures.append("focused contract requested but first_command is missing")
+    else:
+        checks["focused_contract"] = {
+            "status": "SKIPPED",
+            "reason": "use --run-focused-contract to execute current_context.first_command",
+            "command": first_command,
+        }
 
+    after_snapshot = _capture_write_snapshot(repo_root)
+    write_guard = _diff_write_snapshot(before_snapshot, after_snapshot)
+    checks["write_guard"] = write_guard
+    if write_guard.get("changed"):
+        failures.append("preflight changed files before explicit status write")
+    if write_guard.get("status") != "PASS":
+        failures.append("preflight changed files outside allowed boot status output")
+
+    elapsed = round(time.monotonic() - started, 3)
+    if args.mode == "strict" and elapsed > args.max_strict_seconds:
+        warnings.append(f"strict preflight elapsed time {elapsed:.1f}s exceeded target {args.max_strict_seconds:.1f}s")
+
+    verdict = "ERROR" if errors else "FAIL" if failures else "PASS"
     status: dict[str, Any] = {
         "schema_version": SCHEMA_VERSION,
         "generated_at_utc": _utc_now(),
+        "repo_root": str(repo_root),
         "mode": args.mode,
         "require_github": bool(args.require_github),
-        "verdict": "FAIL" if failures else "PASS",
-        "exit_code": 1 if failures else 0,
-        "failures": failures,
+        "run_focused_contract": bool(args.run_focused_contract),
+        "write_status_requested": bool(args.write_status),
+        "verdict": verdict,
+        "exit_code": 2 if errors else 1 if failures else 0,
+        "elapsed_seconds": elapsed,
         "warnings": warnings,
+        "failures": failures,
+        "errors": errors,
         "checks": checks,
+        "next_action": _next_action(verdict, args, failures, errors),
     }
 
-    if args.require_github:
-        post_git = collect_git_state(repo_root)
-        status["post_git"] = post_git
-        if not post_git.get("worktree_clean") or not post_git.get("aligned"):
-            status["verdict"] = "FAIL"
-            status["exit_code"] = 1
-            status["failures"].append("--require-github post-check is not clean/aligned")
-
     if args.write_status:
-        boot_status = make_boot_status_from_preflight(status)
-        write_result = write_boot_status_file(boot_status, args.status_out, repo_root=repo_root)
-        status["status_write"] = {"path": _normalize_path(args.status_out), "result": write_result}
+        if status["verdict"] != "PASS":
+            status["verdict"] = "ERROR"
+            status["exit_code"] = 2
+            status["errors"].append("--write-status is blocked until preflight PASS")
+            status["status_write"] = "blocked-until-pass"
+        else:
+            try:
+                status["status_write"] = write_status_json(
+                    args.status_out,
+                    status,
+                    repo_root=repo_root,
+                    require_github=bool(args.require_github),
+                )
+                post_snapshot = _capture_write_snapshot(repo_root)
+                post_guard = _diff_write_snapshot(before_snapshot, post_snapshot)
+                status["checks"]["post_status_write_guard"] = post_guard
+                if post_guard.get("status") != "PASS":
+                    status["verdict"] = "ERROR"
+                    status["exit_code"] = 2
+                    status["errors"].append("status write left disallowed file mutations")
+                    status["status_write"] = "blocked-post-write-guard"
+            except (StatusWriteConflict, BootStatusValidationError) as exc:
+                status["verdict"] = "ERROR"
+                status["exit_code"] = 2
+                status["errors"].append(str(exc))
+                status["status_write"] = "blocked-status-write-conflict"
+
+    if args.require_github:
+        final_git_state = collect_git_state(repo_root)
+        status["github_final_state"] = {key: value for key, value in final_git_state.items() if key != "entries"}
+        if not _git_ready(status["github_final_state"]):
+            status["verdict"] = "FAIL" if status["verdict"] != "ERROR" else status["verdict"]
+            status["exit_code"] = 1 if status["exit_code"] == 0 else status["exit_code"]
+            if "--require-github final proof detected post-run git drift" not in status["failures"]:
+                status["failures"].append("--require-github final proof detected post-run git drift")
+            status["next_action"] = _next_action(str(status["verdict"]), args, status["failures"], status["errors"])
 
     return status, int(status["exit_code"])
 
 
+def _next_action(verdict: str, args: argparse.Namespace, failures: Sequence[str], errors: Sequence[str]) -> str:
+    if errors:
+        return "Fix preflight configuration or status-write conflict, then rerun."
+    if failures:
+        return "Fix failed boot checks before coding, staging, replay, dashboard, or data work."
+    if args.mode == "planning":
+        return "Planning-ready; run strict before claiming boot-ready."
+    if args.require_github:
+        return "GitHub-aligned strict proof passed read-only."
+    return "Strict preflight passed without final GitHub proof."
+
+
 def render_human(status: Mapping[str, Any]) -> str:
-    boot_status = make_boot_status_from_preflight(status)
+    checks = status.get("checks", {})
     lines = [
         f"BOOT VERDICT: {status.get('verdict')}",
         f"Mode: {status.get('mode')}",
-        f"Boot status: {boot_status.primary_verdict}",
+        f"GitHub required: {status.get('require_github')}",
+        f"Context: {checks.get('context', {}).get('status', 'UNKNOWN') if isinstance(checks.get('context'), Mapping) else 'UNKNOWN'}",
+        f"Data readiness: {checks.get('data_readiness', {}).get('overall_status', 'UNKNOWN') if isinstance(checks.get('data_readiness'), Mapping) else 'UNKNOWN'}",
+        f"Governance: {checks.get('governance', {}).get('status', 'UNKNOWN') if isinstance(checks.get('governance'), Mapping) else 'UNKNOWN'}",
+        f"Write guard: {checks.get('write_guard', {}).get('status', 'UNKNOWN') if isinstance(checks.get('write_guard'), Mapping) else 'UNKNOWN'}",
+        f"Boot-control tests: {checks.get('boot_control_tests', {}).get('status', 'UNKNOWN') if isinstance(checks.get('boot_control_tests'), Mapping) else 'UNKNOWN'}",
     ]
-    failures = status.get("failures") or []
-    warnings = status.get("warnings") or []
-    if failures:
-        lines.append("Failures:")
-        lines.extend(f"- {item}" for item in failures)
-    if warnings:
+    first_command = checks.get("context", {}).get("first_command") if isinstance(checks.get("context"), Mapping) else ""
+    if first_command:
+        lines.extend(["", "First command:", str(first_command)])
+    if status.get("warnings"):
+        lines.append("")
         lines.append("Warnings:")
-        lines.extend(f"- {item}" for item in warnings)
-    lines.append("Deferred:")
-    for _check_id, label, _summary in DEFERRED_DEPENDENCY_CHECKS:
-        lines.append(f"- {label}")
+        lines.extend(f"- {warning}" for warning in status["warnings"])
+    if status.get("failures"):
+        lines.append("")
+        lines.append("Failures:")
+        lines.extend(f"- {failure}" for failure in status["failures"])
+    if status.get("errors"):
+        lines.append("")
+        lines.append("Errors:")
+        lines.extend(f"- {error}" for error in status["errors"])
+    lines.extend(["", f"Next action: {status.get('next_action', '')}"])
+    if status.get("status_write"):
+        lines.append(f"Status write: {status['status_write']}")
     return "\n".join(lines) + "\n"
 
 
 def parse_args(argv: Sequence[str] | None = None) -> argparse.Namespace:
-    parser = argparse.ArgumentParser(description="Terminal Zero boot-core preflight v0")
-    parser.add_argument("--repo-root", default=".", help="Repository root to inspect.")
-    parser.add_argument("--mode", choices=("planning", "strict"), default="planning")
-    parser.add_argument("--strict", action="store_true", help="Alias for --mode strict.")
-    parser.add_argument("--require-github", action="store_true", help="Require clean upstream-aligned Git state.")
-    parser.add_argument("--write-status", action="store_true", help="Write docs/context/boot_status_current.json.")
-    parser.add_argument("--status-out", default=DEFAULT_STATUS_JSON.as_posix())
-    parser.add_argument("--json", action="store_true", help="Print machine-readable preflight JSON.")
-    parser.add_argument("--no-tests", action="store_true", help="Skip strict boot-control pytest gate.")
-    parser.add_argument("--smoke", action="store_true", help="Accepted for compatibility; deferred in boot-core v0.")
+    parser = argparse.ArgumentParser(description="Terminal Zero boot preflight.")
+    parser.add_argument("--repo-root", type=Path, default=Path("."), help="Repository root.")
+    parser.add_argument("--mode", choices=("planning", "strict"), default="strict")
+    parser.add_argument("--planning", dest="mode", action="store_const", const="planning")
+    parser.add_argument("--strict", dest="mode", action="store_const", const="strict")
+    parser.add_argument("--require-github", action="store_true", help="Require clean worktree and upstream-aligned HEAD.")
+    parser.add_argument("--run-focused-contract", action="store_true", help="Run current_context.first_command.")
+    parser.add_argument("--write-status", action="store_true", help="Write runtime/boot_status_current.json.")
+    parser.add_argument("--status-out", type=Path, default=DEFAULT_STATUS_JSON, help="Status JSON output path.")
+    parser.add_argument("--smoke", action="store_true", help="Run the Portfolio route AppTest smoke.")
+    parser.add_argument("--no-tests", action="store_true", help="Skip strict pytest gates.")
     parser.add_argument(
-        "--run-focused-contract",
-        action="store_true",
-        help="Accepted for compatibility; deferred in boot-core v0.",
+        "--max-strict-seconds",
+        type=float,
+        default=DEFAULT_MAX_STRICT_SECONDS,
+        help="Advisory strict-mode runtime target.",
     )
-    args = parser.parse_args(argv)
-    if args.strict:
-        args.mode = "strict"
-    return args
+    parser.add_argument("--json", action="store_true", help="Print the full JSON status instead of human text.")
+    return parser.parse_args(argv)
 
 
 def main(argv: Sequence[str] | None = None) -> int:
-    try:
-        args = parse_args(argv)
-        status, exit_code = build_status(args)
-    except PreflightConfigError as exc:
-        print(f"Boot preflight configuration error: {exc}", file=sys.stderr)
-        return 2
-    except Exception as exc:
-        print(f"Boot preflight internal error: {exc}", file=sys.stderr)
-        return 2
+    args = parse_args(argv)
+    status, exit_code = build_status(args)
     if args.json:
-        print(json.dumps(status, indent=2, sort_keys=True))
+        print(json.dumps(status, indent=2, sort_keys=True), end="\n")
     else:
         print(render_human(status), end="")
     return exit_code
diff --git a/tests/test_boot_preflight.py b/tests/test_boot_preflight.py
index 037fdfc..45225e5 100644
--- a/tests/test_boot_preflight.py
+++ b/tests/test_boot_preflight.py
@@ -3,12 +3,24 @@ from __future__ import annotations
 import json
 from pathlib import Path
 
+import pytest
+
 import launch
-from core.boot_status import BootStatus
+from core.boot_status import BOOT_STATUS_CURRENT_PATH, BootStatus, LEGACY_BOOT_STATUS_PATH
 from scripts import boot_preflight
 from scripts.boot_preflight import CommandResult, DirtyEntry
 
 
+def _ok_context() -> tuple[dict[str, str], CommandResult]:
+    return (
+        {
+            "generated_at_utc": "2026-05-26T07:27:45Z",
+            "first_command": ".venv\\Scripts\\python -m pytest tests/test_strategy_replay.py -q",
+        },
+        CommandResult(args=["python", "scripts/build_context_packet.py", "--validate"], returncode=0),
+    )
+
+
 def _clean_git_state() -> dict[str, object]:
     return {
         "available": True,
@@ -26,6 +38,51 @@ def _clean_git_state() -> dict[str, object]:
     }
 
 
+def _patch_governance_status(monkeypatch: pytest.MonkeyPatch, status: str) -> None:
+    class FakeGovernanceResult:
+        def to_dict(self) -> dict[str, object]:
+            return {
+                "status": status,
+                "passed": status == "PASS",
+                "finding_count": 1 if status != "PASS" else 0,
+                "counts": {},
+                "checks": {},
+                "findings": [],
+            }
+
+    monkeypatch.setattr(boot_preflight, "run_governance_preflight", lambda _repo: FakeGovernanceResult())
+
+
+def _patch_data_readiness_status(monkeypatch: pytest.MonkeyPatch, status: str = "PASS") -> None:
+    monkeypatch.setattr(
+        boot_preflight,
+        "_run_data_readiness_gate",
+        lambda _repo, mode: {
+            "schema_version": "data_readiness_gate.v0",
+            "overall_status": status,
+            "planning_status": status,
+            "strict_status": status,
+            "mode": mode,
+            "summary": {"blockers": ["blocked"] if status == "FAIL" else [], "warnings": [], "next_actions": []},
+        },
+    )
+
+
+def _patch_write_guard(monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_capture_write_snapshot", lambda _repo: {})
+    monkeypatch.setattr(
+        boot_preflight,
+        "_diff_write_snapshot",
+        lambda _before, _after: {
+            "status": "PASS",
+            "changed": [],
+            "allowed_changed": [],
+            "disallowed_changed": [],
+            "post_boot_only_allowed_delta": True,
+        },
+    )
+
+
 def test_generated_expert_packets_are_advisory_evidence() -> None:
     bucket, severity, reason = boot_preflight.classify_path(
         "docs/context/e2e_evidence/reboot_expert_packet_20260526/PACKET_INDEX.md"
@@ -37,25 +94,56 @@ def test_generated_expert_packets_are_advisory_evidence() -> None:
 
 
 def test_dirty_classifier_fails_closed_on_unclassified_source() -> None:
-    summary = boot_preflight.classify_dirty_entries(
-        [DirtyEntry(status="??", path="core/new_runtime_surface.py")]
-    )
+    summary = boot_preflight.classify_dirty_entries([DirtyEntry(status="??", path="core/new_runtime_surface.py")])
 
     assert summary["status"] == "FAIL"
     assert summary["dirty_state"] == "unclassified-source"
     assert summary["blockers"][0]["path"] == "core/new_runtime_surface.py"
 
 
-def test_dirty_classifier_marks_boot_core_files_as_classified() -> None:
+def test_dirty_classifier_keeps_boot_control_source_fail_closed() -> None:
     summary = boot_preflight.classify_dirty_entries(
         [
             DirtyEntry(status="??", path="BOOT.md"),
             DirtyEntry(status="M", path="launch.py"),
+            DirtyEntry(status="M", path="scripts/boot_preflight.py"),
+            DirtyEntry(status="M", path="tests/test_boot_preflight.py"),
+        ]
+    )
+
+    assert summary["status"] == "FAIL"
+    assert summary["counts"]["boot-control-source"] == 4
+    assert {item["path"] for item in summary["blockers"]} == {
+        "launch.py",
+        "scripts/boot_preflight.py",
+        "tests/test_boot_preflight.py",
+    }
+
+
+def test_dirty_classifier_keeps_known_sensitive_source_blocking() -> None:
+    summary = boot_preflight.classify_dirty_entries(
+        [
+            DirtyEntry(status="M", path="dashboard.py"),
+            DirtyEntry(status="M", path="views/detail_view.py"),
+            DirtyEntry(status="M", path="tests/test_provider_ports.py"),
+        ]
+    )
+
+    assert summary["status"] == "FAIL"
+    assert summary["counts"]["known-sensitive-source"] == 3
+
+
+def test_dirty_classifier_marks_root_packet_patch_and_hash_artifacts_advisory() -> None:
+    summary = boot_preflight.classify_dirty_entries(
+        [
+            DirtyEntry(status="??", path="boot_preflight_v0.patch"),
+            DirtyEntry(status="??", path="reboot_expert_packet_20260526_boot_preflight_v0.zip"),
+            DirtyEntry(status="??", path=".boot0a_hash_before.csv"),
         ]
     )
 
     assert summary["status"] == "PASS"
-    assert summary["counts"]["boot-core-candidate"] == 2
+    assert summary["counts"]["generated-evidence"] == 3
 
 
 def test_validate_boot_core_requires_exact_schema_path(tmp_path: Path) -> None:
@@ -72,13 +160,7 @@ def test_validate_boot_core_requires_exact_schema_path(tmp_path: Path) -> None:
                                 "items": {
                                     "properties": {
                                         "status": {
-                                            "enum": [
-                                                "pass",
-                                                "warn",
-                                                "fail",
-                                                "not_applicable",
-                                                "deferred",
-                                            ]
+                                            "enum": ["pass", "warn", "fail", "not_applicable", "deferred"]
                                         }
                                     }
                                 }
@@ -97,50 +179,39 @@ def test_validate_boot_core_requires_exact_schema_path(tmp_path: Path) -> None:
     assert result["required_files"] == list(boot_preflight.BOOT_CORE_REQUIRED_FILES)
 
 
-def test_strict_default_runs_only_boot_control_tests_and_defers_broader_gates(
-    tmp_path: Path,
-    monkeypatch,
-) -> None:
+def test_strict_default_runs_boot_control_tests_not_focused_contract(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
     pytest_commands: list[tuple[str, ...]] = []
 
-    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
-    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
-
-    def fake_pytest_gate(
-        _repo: Path,
-        command_parts: tuple[str, ...],
-        *,
-        timeout: float | None = None,
-    ) -> dict[str, object]:
-        pytest_commands.append(command_parts)
+    def fake_pytest_gate(_repo: Path, command_parts: tuple[str, ...], *, timeout: float | None = None) -> dict[str, object]:
+        pytest_commands.append(tuple(command_parts))
         return {"status": "PASS", "command": "pytest", "returncode": 0}
 
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
     monkeypatch.setattr(boot_preflight, "_run_pytest_gate", fake_pytest_gate)
+    monkeypatch.setattr(boot_preflight, "_run_focused_contract", lambda *_args, **_kwargs: (_ for _ in ()).throw(AssertionError("focused should not run")))
+    _patch_data_readiness_status(monkeypatch)
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
 
-    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--smoke", "--run-focused-contract"])
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict"])
     status, exit_code = boot_preflight.build_status(args)
-    boot_status = boot_preflight.make_boot_status_from_preflight(status)
 
     assert exit_code == 0
     assert status["verdict"] == "PASS"
-    assert pytest_commands == [boot_preflight.BOOT_CONTROL_TEST_COMMAND]
-    assert status["checks"]["data_readiness_gate"]["status"] == "DEFERRED"
-    assert status["checks"]["governance_preflight"]["status"] == "DEFERRED"
-    assert status["checks"]["portfolio_apptest_smoke"]["status"] == "DEFERRED"
-    assert status["checks"]["focused_replay_dashboard_contract"]["status"] == "DEFERRED"
-    assert boot_status.primary_verdict == "degraded"
-    assert boot_status.flags.boot_candidate is True
-    assert boot_status.flags.safe_boot is False
+    assert boot_preflight.BOOT_CONTROL_TEST_COMMAND in pytest_commands
+    assert status["checks"]["focused_contract"]["status"] == "SKIPPED"
 
 
-def test_planning_mode_does_not_run_tests(tmp_path: Path, monkeypatch) -> None:
+def test_planning_mode_does_not_run_tests(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
     monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
     monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
-    monkeypatch.setattr(
-        boot_preflight,
-        "_run_pytest_gate",
-        lambda *_args, **_kwargs: {"status": "FAIL", "command": "should not run"},
-    )
+    monkeypatch.setattr(boot_preflight, "_run_pytest_gate", lambda *_args, **_kwargs: {"status": "FAIL"})
+    _patch_data_readiness_status(monkeypatch)
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
 
     args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--mode", "planning"])
     status, exit_code = boot_preflight.build_status(args)
@@ -149,116 +220,235 @@ def test_planning_mode_does_not_run_tests(tmp_path: Path, monkeypatch) -> None:
     assert status["checks"]["boot_control_tests"]["status"] == "SKIPPED"
 
 
-def test_require_github_blocks_dirty_or_unaligned_state(tmp_path: Path, monkeypatch) -> None:
-    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
-    monkeypatch.setattr(
-        boot_preflight,
-        "collect_git_state",
-        lambda _repo: {**_clean_git_state(), "aligned": False, "ahead": 1},
+def test_focused_contract_rejects_shell_metacharacters(tmp_path: Path) -> None:
+    result = boot_preflight._run_focused_contract(
+        tmp_path,
+        ".venv\\Scripts\\python -m pytest tests/test_strategy_replay.py -q && echo unsafe",
     )
-    monkeypatch.setattr(
-        boot_preflight,
-        "_run_pytest_gate",
-        lambda *_args, **_kwargs: {"status": "PASS", "command": "pytest", "returncode": 0},
+
+    assert result["status"] == "FAIL"
+    assert result["returncode"] == 2
+    assert "shell metacharacters" in result["stderr_tail"]
+
+
+def test_focused_contract_runs_without_shell_with_timeout(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    seen: list[tuple[object, bool, float | None]] = []
+
+    def fake_run_command(args: object, *, cwd: Path, shell: bool = False, timeout: float | None = None) -> CommandResult:
+        seen.append((args, shell, timeout))
+        return CommandResult(args=args, returncode=0)
+
+    monkeypatch.setattr(boot_preflight, "_run_command", fake_run_command)
+
+    result = boot_preflight._run_focused_contract(
+        tmp_path,
+        ".venv\\Scripts\\python -m pytest tests/test_strategy_replay.py -q",
+        timeout=12.5,
     )
 
-    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--require-github"])
+    assert result["status"] == "PASS"
+    assert seen[0][1] is False
+    assert seen[0][2] == 12.5
+
+
+def test_governance_warn_status_is_advisory(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
+    monkeypatch.setattr(boot_preflight, "_run_pytest_gate", lambda *_args, **_kwargs: {"status": "PASS", "command": "pytest", "returncode": 0})
+    _patch_data_readiness_status(monkeypatch)
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "WARN")
+
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--no-tests"])
+    status, exit_code = boot_preflight.build_status(args)
+
+    assert exit_code == 0
+    assert status["verdict"] == "PASS"
+    assert "governance preflight warning" in status["warnings"]
+
+
+def test_governance_fail_blocks_strict(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
+    monkeypatch.setattr(boot_preflight, "_run_pytest_gate", lambda *_args, **_kwargs: {"status": "PASS", "command": "pytest", "returncode": 0})
+    _patch_data_readiness_status(monkeypatch)
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "FAIL")
+
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--no-tests"])
     status, exit_code = boot_preflight.build_status(args)
 
     assert exit_code == 1
-    assert "--require-github requires HEAD to match upstream" in status["failures"]
+    assert "governance preflight did not pass: FAIL" in status["failures"]
 
 
-def test_make_boot_status_from_preflight_is_schema_valid() -> None:
-    preflight_status = {
-        "schema_version": boot_preflight.SCHEMA_VERSION,
-        "generated_at_utc": "2026-05-26T00:00:00Z",
-        "mode": "strict",
-        "require_github": False,
-        "verdict": "PASS",
-        "exit_code": 0,
-        "warnings": [],
-        "checks": {
-            "boot_core": {"status": "PASS", "command": "file-contract"},
-            "git": _clean_git_state(),
-            "dirty": {"status": "PASS", "dirty_state": "clean"},
-            "boot_control_tests": {"status": "PASS", "command": "pytest"},
-        },
-    }
+def test_strict_data_readiness_warn_blocks_boot(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
+    _patch_data_readiness_status(monkeypatch, "WARN")
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
+
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--no-tests"])
+    status, exit_code = boot_preflight.build_status(args)
+
+    assert exit_code == 1
+    assert "data readiness gate status blocks strict boot: WARN" in status["failures"]
 
-    boot_status = boot_preflight.make_boot_status_from_preflight(preflight_status)
-    round_trip = BootStatus.from_json_dict(json.loads(boot_status.to_json_text()))
 
-    assert round_trip.primary_verdict == "degraded"
-    assert any(check.id == "data_readiness_gate" and check.status == "deferred" for check in round_trip.checks)
+def test_planning_data_readiness_warn_is_advisory(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
+    _patch_data_readiness_status(monkeypatch, "WARN")
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
 
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--mode", "planning"])
+    status, exit_code = boot_preflight.build_status(args)
 
-def test_launch_preflight_dispatch_adds_repo_root(monkeypatch) -> None:
-    seen: list[list[str]] = []
+    assert exit_code == 0
+    assert "data readiness gate status: WARN" in status["warnings"]
 
-    def fake_main(argv: list[str]) -> int:
-        seen.append(argv)
-        return 17
 
-    monkeypatch.setattr(boot_preflight, "main", fake_main)
-    monkeypatch.setattr(launch, "_project_root", lambda: "E:\\Code\\Quant")
+def test_require_github_fails_when_branch_is_not_aligned(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: {**_clean_git_state(), "upstream_head": "def456", "ahead": 1, "aligned": False})
+    monkeypatch.setattr(boot_preflight, "_run_pytest_gate", lambda *_args, **_kwargs: {"status": "PASS", "command": "pytest", "returncode": 0})
+    _patch_data_readiness_status(monkeypatch)
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
 
-    result = launch._run_preflight(["--preflight", "--strict"])
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--require-github"])
+    status, exit_code = boot_preflight.build_status(args)
 
-    assert result == 17
-    assert seen == [["--repo-root", "E:\\Code\\Quant", "--strict"]]
+    assert exit_code == 1
+    assert any("local HEAD to equal upstream HEAD" in item for item in status["failures"])
+
+
+def test_make_boot_status_safe_boot_requires_github_focused_and_smoke() -> None:
+    checks = {
+        "boot_core": {"status": "PASS"},
+        "context": {"status": "PASS"},
+        "git": _clean_git_state(),
+        "dirty": {"status": "PASS"},
+        "data_readiness": {"overall_status": "PASS"},
+        "governance": {"status": "PASS"},
+        "write_guard": {"status": "PASS"},
+        "boot_control_tests": {"status": "PASS"},
+        "portfolio_smoke": {"status": "SKIPPED"},
+        "focused_contract": {"status": "SKIPPED"},
+    }
+    base = {
+        "schema_version": "boot-preflight.v1",
+        "generated_at_utc": "2026-05-26T00:00:00Z",
+        "mode": "strict",
+        "require_github": True,
+        "run_focused_contract": False,
+        "verdict": "PASS",
+        "warnings": [],
+        "failures": [],
+        "checks": checks,
+        "github_final_state": _clean_git_state(),
+    }
 
+    candidate = boot_preflight.make_boot_status_from_preflight(base)
+    safe = boot_preflight.make_boot_status_from_preflight(
+        {
+            **base,
+            "run_focused_contract": True,
+            "checks": {
+                **checks,
+                "portfolio_smoke": {"status": "PASS"},
+                "focused_contract": {"status": "PASS"},
+            },
+        }
+    )
 
-def test_launch_preflight_strict_alias_cannot_be_downgraded(monkeypatch) -> None:
-    seen: list[list[str]] = []
+    assert candidate.flags.boot_candidate is True
+    assert candidate.flags.safe_boot is False
+    assert candidate.primary_verdict == "degraded"
+    assert safe.flags.safe_boot is True
+    assert safe.primary_verdict == "ready"
 
-    def fake_main(argv: list[str]) -> int:
-        seen.append(argv)
-        return 0
 
-    monkeypatch.setattr(boot_preflight, "main", fake_main)
-    monkeypatch.setattr(launch, "_project_root", lambda: "E:\\Code\\Quant")
+def test_write_status_defaults_to_runtime_canonical_path(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
+    monkeypatch.setattr(boot_preflight, "_run_pytest_gate", lambda *_args, **_kwargs: {"status": "PASS", "command": "pytest", "returncode": 0})
+    _patch_data_readiness_status(monkeypatch)
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
 
-    assert launch._run_preflight(["--preflight-strict", "--mode", "planning"]) == 0
-    assert seen == [["--repo-root", "E:\\Code\\Quant", "--mode", "planning", "--mode", "strict"]]
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--write-status"])
+    status, exit_code = boot_preflight.build_status(args)
 
+    assert exit_code == 0
+    assert status["status_write"] == "written"
+    assert (tmp_path / BOOT_STATUS_CURRENT_PATH).exists()
+    assert not (tmp_path / LEGACY_BOOT_STATUS_PATH).exists()
 
-def test_launch_preflight_enforces_project_venv(monkeypatch) -> None:
-    monkeypatch.setattr(launch, "_is_preflight_request", lambda _argv: True)
-    monkeypatch.setattr(launch, "_check_python_version", lambda: [])
-    monkeypatch.setattr(launch, "_check_venv", lambda: ["Python executable is not from project .venv."])
-    monkeypatch.setattr(launch, "_print_errors", lambda _errors: None)
-    monkeypatch.setattr(launch, "_run_preflight", lambda _argv: 0)
 
-    assert launch.main() == 1
+def test_write_status_blocks_until_preflight_passes(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
+    _patch_data_readiness_status(monkeypatch, "FAIL")
+    _patch_write_guard(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
 
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--write-status"])
+    status, exit_code = boot_preflight.build_status(args)
 
-def test_no_boot_core_imports_for_deferred_dependency_modules() -> None:
-    source = Path(boot_preflight.__file__).read_text(encoding="utf-8")
+    assert exit_code == 2
+    assert status["status_write"] == "blocked-until-pass"
+    assert not (tmp_path / BOOT_STATUS_CURRENT_PATH).exists()
 
-    assert "from scripts.governance_preflight" not in source
-    assert "import scripts.governance_preflight" not in source
-    assert "core.data_readiness_gate" not in source
 
+def test_write_status_runs_post_write_guard_and_blocks_disallowed_delta(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setattr(boot_preflight, "_validate_context", lambda _repo: _ok_context())
+    monkeypatch.setattr(boot_preflight, "validate_boot_core", lambda _repo: {"status": "PASS", "blockers": []})
+    monkeypatch.setattr(boot_preflight, "collect_git_state", lambda _repo: _clean_git_state())
+    monkeypatch.setattr(boot_preflight, "_run_pytest_gate", lambda *_args, **_kwargs: {"status": "PASS", "command": "pytest", "returncode": 0})
+    _patch_data_readiness_status(monkeypatch)
+    _patch_governance_status(monkeypatch, "PASS")
+    snapshots: list[dict[str, tuple[int, int]]] = [{}, {}, {"runtime/boot_status_current.json": (1, 1), "data/runtime_cache/replay.tmp": (1, 1)}]
+    monkeypatch.setattr(boot_preflight, "_capture_write_snapshot", lambda _repo: snapshots.pop(0))
+
+    def fake_diff(_before: dict[str, tuple[int, int]], after: dict[str, tuple[int, int]]) -> dict[str, object]:
+        if "data/runtime_cache/replay.tmp" in after:
+            return {
+                "status": "FAIL",
+                "changed": ["runtime/boot_status_current.json", "data/runtime_cache/replay.tmp"],
+                "allowed_changed": ["runtime/boot_status_current.json"],
+                "disallowed_changed": ["data/runtime_cache/replay.tmp"],
+                "post_boot_only_allowed_delta": False,
+            }
+        return {"status": "PASS", "changed": [], "allowed_changed": [], "disallowed_changed": [], "post_boot_only_allowed_delta": True}
+
+    monkeypatch.setattr(boot_preflight, "_diff_write_snapshot", fake_diff)
+
+    args = boot_preflight.parse_args(["--repo-root", str(tmp_path), "--strict", "--write-status"])
+    status, exit_code = boot_preflight.build_status(args)
 
-def test_pytest_gate_uses_argv_not_shell(tmp_path: Path, monkeypatch) -> None:
-    seen: list[tuple[object, bool, float | None]] = []
+    assert exit_code == 2
+    assert status["status_write"] == "blocked-post-write-guard"
+    assert "status write left disallowed file mutations" in status["errors"]
 
-    def fake_run_command(
-        args: object,
-        *,
-        cwd: Path,
-        shell: bool = False,
-        timeout: float | None = None,
-    ) -> CommandResult:
-        seen.append((args, shell, timeout))
-        return CommandResult(args=args, returncode=0)
 
-    monkeypatch.setattr(boot_preflight, "_run_command", fake_run_command)
+def test_launch_preflight_dispatches_before_environment_checks(monkeypatch: pytest.MonkeyPatch) -> None:
+    seen_args: list[list[str]] = []
 
-    result = boot_preflight._run_pytest_gate(tmp_path, boot_preflight.BOOT_CONTROL_TEST_COMMAND, timeout=12.5)
+    def fake_main(argv: list[str]) -> int:
+        seen_args.append(argv)
+        return 7
 
-    assert result["status"] == "PASS"
-    assert seen
-    assert seen[0][1] is False
-    assert seen[0][2] == 12.5
+    monkeypatch.setattr(boot_preflight, "main", fake_main)
+
+    assert launch._run_preflight(["--preflight", "--strict", "--no-tests"]) == 7
+    assert seen_args == [["--repo-root", str(Path(launch._project_root())), "--strict", "--no-tests"]]
