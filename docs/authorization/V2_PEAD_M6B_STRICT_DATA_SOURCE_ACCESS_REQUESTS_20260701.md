# V2 PEAD M6b Strict Data Source-Access Requests

Status: `REQUEST_PREPARED_EVIDENCE_MISSING`  
RoundID: `ROUND-20260701-V2-PEAD-M6B-STRICT-DATA-PHASE0-SUCCESSOR`  
ScopeID: `V2_PEAD_M6B_STRICT_DATA_PHASE0_DOCS_ONLY`  
Authority: Request preparation only; not source-access authorization, entitlement proof, export authority, archive inspection authority, provider access, factual gate validation, data output, or strict readiness authority.

## Historical lineage

- Supersedes: `docs/authorization/V2_PEAD_M6B_STRICT_DATA_SOURCE_ACCESS_REQUESTS_20260630.md`
- Prior Markdown SHA-256: `a2b2c99c840abff972bedda1690b3f79fe036a3225a4250d28c6e2a8e37e7e3c`
- Prior machine request: `docs/authorization/V2_PEAD_M6B_STRICT_DATA_SOURCE_ACCESS_REQUESTS_20260630.json`
- Prior machine-request SHA-256: `fdf315a1ee8ab5a0ef3db3e02ac54823bd93d1f9ad450a2eb6369d12ace98b74`
- Historical artifacts are preserved without modification.

The authoritative machine-readable successor is:

`docs/authorization/V2_PEAD_M6B_STRICT_DATA_SOURCE_ACCESS_REQUESTS_20260701.json`

## Global boundary

- Licensed raw data remains outside Git unless a data owner explicitly authorizes raw-byte storage.
- Agents may inspect only named local bytes after a separate source-access authorization.
- No `secret.txt`, credential, provider, API, network, download, export, extraction, ETL, data generation, Best-Available B reuse, curve, return, alpha, strategy, or readiness work is authorized.

## Gate A — strict PIT-family, timing, and calendar request

The only active Gate A contract is:

`docs/authorization/V2_PEAD_M6B_GATE_A_EPS_DEFINITION_CONTRACT_REQUEST_20260701.json`

Active contract SHA-256: `27a065e5a37d44acd5e423e448d0a894274b48215eb0bcfc32968d5ba5931063`

Data-owner approval must include a source-capability attestation for exact licensed first-public, revision/status, timing, timezone, and mapping fields. It must also bind an immutable market-calendar artifact. A separate timing artifact is required only when the selected PIT record lacks a complete eligible timestamp with unambiguous timezone semantics; then the join must be one-to-one.

The request must name exact licensed views/fields, authorized outside-Git local artifacts, source-as-of/export timestamps, dictionaries, manifests, row counts, hashes, and local-byte-only inspection terms.

Gate A remains `BLOCKED_STRICT_PIT_ARTIFACT_MISSING` until those requirements are met. `comp_fundq` is not a strict substitute; `rdq` is cross-check-only.

## Gate B — candidate input request

Status: `CANDIDATE_LOCAL_INPUT_PRESENT__AUTHORIZATION_AND_FACTUAL_VERIFICATION_PENDING`.

Request a separately authorized CRSP return/delisting archive with exact licensed dictionary support for security return, delisting event/return, date coverage, manifest, and hashes. This is only a request for factual sufficiency review; it does not make Gate B pass-like and does not authorize derived return output.

## Gate C — hard attribute-scope gap

Status: `BLOCKED_ATTRIBUTE_SCOPE_UNVERIFIED`.

Request a separately authorized CRSP market-attribute/archive package with date-ranged active listing/trading status, price, volume, shares, corporate-action eligibility, mappings, and date-ranged `EXCHCD`/`SHRCD` or a licensed-dictionary-documented equivalent with identical decision-time semantics.

`COMNAM`, current CUSIP, ticker, or inferred identity are not substitutes.

## Gate D — deferred integration gap

Status: `DEFERRED_SOURCE_INDEPENDENT_INTEGRATION_GAP`.

No securities-finance archive is requested now. A separate approved interface audit must first establish that an existing canonical consumer and test/reference prove dated availability, `bps_per_day` cost, threshold exclusion, and missing-borrow fail-close enforcement.

## Next action

Obtain separate data-owner responses for the Gate A and Gate B/C source-access requests. Do not inspect any archive until the corresponding authorization, named immutable artifacts, and fresh reviewer capacity exist.
