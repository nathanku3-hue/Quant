# GV-V2 Roadmap Custody Repair Brief

Date: 2026-07-29
Mode: `CLOSURE_REPORT`
Status: `R0_BANKED; INDEPENDENT_AUDIT_PENDING`
Authority: corrected roadmap, active-brief selector, and repository custody evidence

## Recommended next action

Independently audit the exact `ROADMAP_FREEZE_COMMIT`. After PASS, create one clean isolated implementation worktree from that commit and execute `GV-MICRO-PORTFOLIO-VERTICAL-0`. Do not create an implementation branch from raw `93e7a55`.

## R0 decision

```text
REMOVE STANDALONE GV-CANON-RESET-0 FROM PRODUCT SEQUENCE
BANK ONE CHECKOUTABLE ROADMAP AUTHORITY
SELECT ACTIVE BRIEF EXPLICITLY
PRESERVE RELEASED GV_FS0_V1
SHIP MICRO-PORTFOLIO FIRST
CERTIFY REPLAY BEFORE SCALE
```

## Delivered custody repair

- stale direct-base instructions are explicitly superseded;
- `docs/context/ACTIVE_BRIEF` selects the active nonnumeric product brief;
- default context generation fails closed when the pointer is missing or invalid;
- numeric phase discovery remains migration-only behind `--allow-legacy-discovery`;
- Phase 66 bridge moved to archive;
- root product, architecture, context, decision, lesson, SAW, and handover surfaces are reconciled;
- released Alpha/FS0 remains unchanged;
- root checkout remains untouched.

## Product sequence

```text
GV-MICRO-PORTFOLIO-VERTICAL-0
→ GV-DETERMINISTIC-REPLAY-0
→ GV-BOUNDED-PORTFOLIO-1
→ GV-PORTFOLIO-SCALE-1
→ GV-UNIVERSE-SCALE-1
→ GV-CHALLENGER-PROMOTION-1
→ GV-LIMITED-LIVE-1
```

Only the first vertical and replay are implementation-authorized after independent R0 audit.

## Execution topology

- Package A — Truth core.
- Package B — Decision vertical.
- Package C — Product closure.

Minimum identity/event seams freeze before parallel work. Detailed fields freeze only when the operator fixture exercises them.

## Score treatment

Canonical shipped score remains `39/100`; observed comparisons remain `0`; no alpha claim.

```text
Roadmap custody banked             1/1
Micro-portfolio operator loop      0/1
Prospective later observation      0/1
Exact deterministic replay         0/1
Bounded repeated portfolio         0/1
```

Forecasts are planning aids only: weighted audit maturity may reach `62–66` after the first vertical and `70–74` after exact replay, but neither range is evidence until acceptance is banked.

## P0/P1 risks

| Risk | Severity | Control |
|---|---|---|
| stale implementation base | P0 | branch only from audited `ROADMAP_FREEZE_COMMIT` |
| dirty root contamination | P0 | isolated worktree only; root untouched |
| schema catalogue delays product | P1 | fixture-earned field detail |
| package interface divergence | P1 | minimum seams frozen first |
| replay delayed until after scale | P1 | replay immediately follows real vertical events |

## Forbidden scope

providers · WRDS acquisition · broad historical loaders · released FS0 mutation · optimizer · copula · graph propagation · adaptive execution · tactical capital · broad tax/FX · shorting · leverage · derivatives · broker · live capital · score uplift · alpha claim

## Stop rules

1. Stop if independent audit does not reproduce the banked commit and validation evidence.
2. Stop if a new worktree is not descended from the exact roadmap commit.
3. Stop if a proposed gate does not protect PIT validity, accounting integrity, mandatory actions, mandate safety, or replay.

Active product brief: `docs/phase_brief/phase0-gv-micro-portfolio-vertical-0-brief.md`.
