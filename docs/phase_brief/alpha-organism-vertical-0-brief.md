# ALPHA-ORGANISM-VERTICAL-0 — Active Brief

Date: 2026-08-08
Branch: `codex/pit-source-authority-1`
Status: `PRE_SEAL_REAL_CIQ_ADMISSION; V3 TEMPORAL + ADVERSARIAL AUTHORITY CLOSED LOCALLY; CIQ IDENTITY + PRIMARY MASTER RAW CUSTODY SOLVED; REAL CLOCK BLOCKED ON >=200-DAY PRIMARY-SPT MARKET CUSTODY + ADMISSION; LIVE CLOSED`
Episode-2 local immutable candidate: `39f7be3894623c095994066b8f0ea2895b968643`
AOV local executable tip: `dca69fc72dd3192913aa921323ff48f68610a925`
Canonical product maturity: `70/100` (accepted operability/custody/replay only)
Portfolio-alpha evidence: `0`
Limited Live: `CLOSED; NOT AUTHORIZED`

## Product result

The sole active product milestone is the **first verified prospective Clock-Start Receipt** under destructive v3 authority:

```text
admitted PIT vertical cube
→ Rule100 control
→ deterministic CFTD Parent
→ one frozen risk-reducing Child
→ PIT equal weight
→ economic cash
→ immutable same-cut Seal Candidate v3
→ fresh-process full-chain verification
→ immutable PROSPECTIVE_CLOCK_START_RECEIPT v1
```

Architecture completion, a Seal Candidate, test fixtures, or historical mechanics do not start the prospective clock and do not earn financial-alpha evidence.

## Gate A — Episode-2 local predecessor

Local custody is complete:

- [x] exact Episode-2 runtime/test/data pathset committed at `39f7be3`;
- [x] exact `git archive` reconstruction performed;
- [x] exact selected matrix collects `115` tests;
- [x] exact archived bytes pass `115/115`;
- [x] stale `142/142` receipt identified and superseded.

External custody is deliberately still open:

- [ ] push exact candidate SHA;
- [ ] hosted Windows exact-head proof;
- [ ] hosted Ubuntu exact-head proof;
- [ ] independent cross-domain audit;
- [ ] owner-authorized fast-forward/tag publication.

Those external actions were not authorized in this round and do not reopen local Gate-B engineering.

## Gate B — hard cut + AOV vertical

Implemented locally:

- [x] four duplicate root app/launcher surfaces removed from current authority;
- [x] historical Alpha runtime/build code moved to `docs/archive/legacy_runtime_source/`;
- [x] historical Alpha release converted to receipt-integrity authority rather than current-root rebuild compatibility;
- [x] `dashboard.py` reduced to one canonical authority shell;
- [x] legacy book projection removed;
- [x] ZERO-COMPAT scan passes all seven counters at zero, including the archived/release executable-source import guard;
- [x] five research-spine defects repaired without compatibility shims;
- [x] minimal `VERTICAL-CUBE-SLICE-V0` implemented;
- [x] deterministic Rule100 / Parent / one Child policy implemented;
- [x] hash-addressed DAG and selective recomputation implemented;
- [x] five-arm experiment/evidence machinery implemented;
- [x] prospective custody hardened to v2: actual seal-write time is independently system-stamped, first execution is validated against frozen NYSE session/core-open authority, exact executable/interpreter bytes are bound, and fresh-process full-chain reopen is mandatory;
- [x] first-seal decision cut hash-binds all four Parquet inputs, frozen contract hash, mechanically recomputed date-local universe hash, and four required raw-source receipts/retrieval times/hashes; enforces knowledge/target/execution chronology plus NY Fed retrieval at/after 15:00 ET; re-hashes inputs before and after the run; and seals current target-vector hashes for all five arms;
- [x] deterministic Parent–Child review core implemented;
- [x] official-SOFR economic cash construction implemented under existing owner authority;
- [x] bounded CIQ Security/market admission builder implemented with canonical `CIQSEC:` identity, mechanical exclusion, current-cut-only Rule100 targets, same-cut total returns, and market-derived primitives; same-day U.S. daily market rows retrieved before 16:00 America/New_York and future-stamped source retrieval times block before output;
- [x] direct NY Fed SOFR intake implemented with a hard pre-network 15:00 America/New_York gate and raw-byte receipt;
- [x] deterministic decision-cut builder implemented; it binds the four active input/source receipts with no `run_2`/screen timestamp parameter and requires target-date Rule100/return/primitive asset-set equality plus exact primitive-vs-P&L total-return reconciliation; `scripts/aov0_first_seal.py` independently enforces the same return equality before execution;
- [x] destructive v3 authority implemented: `decision_cut_v3`, `seal_v3`, close-based evaluation, clock-false Seal Candidate, fresh-process verification proof, and separate immutable Clock-Start Receipt;
- [x] mandatory adversarial v3 gate covers market-byte tamper, +1bp serialized target mutation, Security-ID/ticker mutation, SOFR substitution, same-process promotion denial, weekend/wrong-close/legacy-open/pre-cut timing, pre-evaluation return attribution, early maturity, and temporal unavailability before receipt/evaluation/maturity;
- [x] synthetic current-cut CIQ → SOFR → v3 decision cut reaches Seal Candidate → fresh-process `FULL_CHAIN_REOPEN_VERIFIED` proof → separate Clock-Start Receipt with financial-alpha evidence `0`;
- [x] local AOV suite passes `75/75`; ZERO-COMPAT remains seven zeros and active runtime has no v2/open authority references;
- [x] CIQ provider capability proved through the installed authenticated S&P Office client: direct Genix auth/HTTP/serialization returns a valid provider response, while the supported worksheet `SPGTable` path returns exact-date `SP_TOTAL_RETURN`, `SP_PRICE_CLOSE`, and `SP_VOLUME`;
- [x] historical as-of fundamentals capability proved through `SPG(..., as_of_date, ...)`; this is banked post-Clock research capability only and does not convert `run_4` into historical PIT authority or authorize Parent/Child tuning;
- [x] provider identity layers proved and frozen: Identifier Lookup exposes primary `SPT344984472` and alternate `SPT364472819` for `COE`; both share `SP_CIQ_ID=IQ337968870` while `SP_TRADING_ITEM_ID` differs, proving security-level vs listing-level identity. Direct company-key `SPGTable` returns the same primary pair without per-name UI traversal;
- [x] real 109-name primary Security/Trading Item raw master captured at `data/aov0/raw/ciq_primary_security_master_20260808T162322Z.csv`, retrieval `2026-08-08T16:23:22.0736860Z`, SHA-256 `8aefbbd751a714b8689402ccbf8fa2b776c6388d4bbc3870ec4f8b975306eca4`; all 109 identity pairs are present and unique, with provider ticker/exchange matching the frozen universe; raw custody is complete but admission remains open;
- [x] market extraction operating boundary proved: one fresh Office process per atomic part; 5/6/7 weekdays × 109 names × 3 exact fields are stable, 8/10-day widths fail at the bounded window, and exact-primary-SPT 6-day data equals company-primary data excluding retrieval timestamp. Seven-weekday exact-SPT chunks are the incumbent; nine earlier 5-day parts plus a validated 7-day target-week part are already custody;
- [x] acquisition dead ends classified and frozen: embedded SNLQuery/synthetic-provider execution hangs; legacy `SNLPrice` stalled and was cancelled; direct raw Genix `SPGTable` normalization work is unnecessary; fake generic `SP_SECURITY_ID` provider semantics, ticker fallback, and widening beyond the proven 7-day chunk are not incumbents. Detailed findings are in `docs/context/ciq_provider_acquisition_findings_20260808.md`.

## Executable P0 contract

### Frozen

1. `F_proxy = robust_z(sign(total_return) × min(abs(total_return)/realized_vol,3) × dollar_volume/ADV20)`.
2. `C_proxy = EWMA20(abs(F_proxy))`; no second ADV division.
3. Permanent active AOV identity is S&P Capital IQ Security ID, canonical `CIQSEC:<id>`; ticker/asset aliases, company `SP_ENTITY_ID`, legacy PERMNO, and dual-provider identity are invalid on the AOV path.
4. Universe is the date-local Rule100 eligible universe.
5. PIT total-return matrix is the sole P&L authority; corporate actions are reconciliation-only.
6. Parent preserves Rule100 risky gross budget, cap, decision schedule, and residual-cash behavior; no forced full-investment softmax.
7. Rule100 degenerate equivalence tolerance is frozen in the executable contract.
8. One V0 engineering configuration is frozen; no result-driven calibration.
9. Child differs from Parent by one frozen reversal-insurance mutation and may only reduce risk/increase cash.
10. Execution uses one-bar lag; attempts are weekly; the experimental horizon is fixed at 30 calendar days.
11. Inference uses paired weekly Child-minus-Parent net-return evidence with overlap-aware weekly HAC/block structure.
12. Economic cash inherits the existing owner mandate: official SOFR minus 25 bp, ACT/360, no zero floor, usable only after official publication, no proxy substitution.
13. Insurance primary endpoint is Expected Shortfall / CVaR.

### Insurance V0 owner decision — frozen

Production authority is now:

- [x] `insurance_materiality_floor_ratio = 0.05`;
- [x] `insurance_premium_ceiling_annual_return = 0.0015`.

This requires at least 5% relative CVaR/Expected-Shortfall improvement while permitting at most 15 bp/year expected net-return sacrifice. Development tests may still inject alternate values to prove mechanics, but changing either production value creates a new contract/model family; subsequent results do not calibrate V0 in place.

## Research-spine hardening

Closed defects:

- [x] evidence/frame identity hashes actual content;
- [x] headline benchmark is explicitly named rather than dict/list-order dependent;
- [x] NaN/non-finite costs are invalid;
- [x] benchmark contract is named/closed and requires cash + PIT-EW + economic cash;
- [x] PIT-EW follows the same strategy decision/rebalance schedule;
- [x] Rule100 adapter requires permanent IDs, daily-portfolio semantics, and reconciled cash;
- [x] evidence run directories are immutable and manifest-hash-bound.

## First real prospective clock — blocked only on real CIQ data admission

`python scripts/aov0_first_seal.py` currently returns:

```text
status = BLOCKED_MISSING_ADMITTED_INPUTS
prospective_clock_started = false
financial_alpha_evidence = 0
```

Current real artifacts still required after direct SOFR admission:

- `data/aov0/current/rule100_targets.parquet`;
- `data/aov0/current/vertical_primitives.parquet`;
- `data/aov0/current/total_returns.parquet`;
- `data/aov0/current/decision_cut.json`.

`data/aov0/current/official_sofr.parquet` is already admitted from the direct NY Fed response retrieved at `2026-08-07T19:00:08.894288Z`, raw SHA-256 `445ca1ae93a7ae904681716d8e37088fab905ae4f74f32fc2619a459918d54cc`, Parquet SHA-256 `ed75219416a524f17cb3e29b9e4fadff2dcfa1d12a8368d752007aac779c4e5e`.

The prior custody findings and both later temporal-authority defects are now closed locally. Active v3 uses the next eligible NYSE 16:00 ET close as `evaluation_start`; Seal construction is clock-false; a distinct fresh-process verification proof is required; only a separate immutable Clock-Start Receipt may return clock authority; attributed daily-return intervals must begin at/after evaluation start; maturity is exactly evaluation start + 30 calendar days. Active v2/open readers and writers are absent.

No historical equal-weight replay, ticker-only artifact, zero-return cash, or provider substitute may fill these gaps. `run_4` is current-cut-only, so the CIQ market builder intentionally uses historical market rows only to warm ADV20/realized-volatility/SMA20/SMA200/AOV state and emits one current Rule100 target row plus one matching current total-return row; it does not backcast the current factors. Ambiguous/missing primary identity, cross-entity identity collisions, factor coverage below three groups, insufficient 200-day market history, or invalid target state exclude the name rather than opening a compatibility path. For this U.S.-listed daily cut, a target on the market-retrieval date is invalid before 16:00 America/New_York; future-stamped retrieval times also block.

The remaining pre-clock round must not widen scope. Provider capability **and primary identity are already solved**. Reuse and verify the captured 109-name raw master `data/aov0/raw/ciq_primary_security_master_20260808T162322Z.csv` (SHA-256 `8aefbbd751a714b8689402ccbf8fa2b776c6388d4bbc3870ec4f8b975306eca4`). Complete the remaining historical market custody with exact primary SPT query keys in 7-weekday atomic chunks using `SP_TOTAL_RETURN`, `SP_PRICE_CLOSE`, and `SP_VOLUME`; 7 weekdays is the largest proven stable width and 8/10-day widths failed. Nine earlier 5-day parts plus the validated target-week exact-SPT 7-day part are already in custody. Combine only real provider part CSVs, dedupe on (`SPT_DATE`,`SP_CIQ_ID`,`SP_TRADING_ITEM_ID`), retain source metrics and retrieval timestamps, compute the final raw hash, and verify actual completed observations per security. The authoritative admission gate is >=200 completed closes through `2026-08-07` after the existing factor-coverage filter, not a requested weekday count; genuinely shorter histories are excluded without alternate-listing backfill. The `.venv` directory exists at handover but `.venv/Scripts/python.exe` is absent, so restore the approved `.venv` interpreter before running Python and do not substitute system Python. Then run the existing fail-closed CIQ builder → `decision_cut_v3` → real Seal Candidate → fresh-process verification proof → immutable Clock-Start Receipt. Do not retry embedded SNLQuery/SNLPrice/SPGRANGE or tune Parent/Child from historical PIT probes. Preserving 09:30 execution would require a different future contract family with authoritative open/fill interval data and is not active.

## Deterministic review status

Implemented now:

- [x] Parent–Child gross/cost/net reconciliation with tolerance;
- [x] accounting failure blocks review authority;
- [x] deterministic statuses for helped, hurt, cost-dominated, insufficient evidence, accounting failure;
- [x] exact review-packet hash verification;
- [x] single episode has no structural mutation authority;
- [x] synthetic helped/hurt/cost/accounting/non-finite/tamper fixtures.

Still to close while the real clock runs:

- [ ] full score→target→executed-weight→P&L lineage packet;
- [ ] global-redistribution fixture;
- [ ] regime-transition fixture;
- [ ] corporate-action/total-return reconciliation fixture;
- [ ] deterministic cohort/regime packet and B0 ontology completion.

## North-star sequence

```text
v3 temporal authority [CLOSED LOCAL]
+ adversarial authority tests [CLOSED LOCAL]
+ admit current real CIQ inputs [OPEN]
→ immutable five-arm SEAL CANDIDATE
→ fresh-process full-chain verify
→ immutable PROSPECTIVE_CLOCK_START_RECEIPT
→ weekly frozen-109 tape never stops
│
├─ one active Alpha build lane
│  first lane = CYCLE_RESONANCE_v1 [PREREGISTERED / NOT IMPLEMENTED]
│  Minimum Viable Atlas → freeze → untouched test where available → seal prospectively
│  deeper Atlas = discovery-only right-tail episode census, not famous-company survivor selection
│  retain true-right-tail / false-winner / missed-right-tail / matched ordinary-or-left controls
│  audit incumbent winner-blindness without tuning; separate entry / continuation / exit alpha
│  while deeper Atlas / replication work continues underneath the running tape
├─ deterministic review/custody closure
├─ thin Alpaca PAPER Capitalization Vertical immediately after Clock #1 under independent ownership
│  initial execution incumbent = MOC_CLOSE_AUCTION_V1 (market + cls)
│  current frozen AOV targets may exercise account/map/lifecycle/restart/IS paths with financial_alpha_evidence=0
├─ independent-replication data lead time after first Challenger seal
└─ borrow entitlement lead time asynchronously

first capital-relevant edge
→ exactly one promoted policy/seal → one live_rebalance_id
→ simple allocation incumbent OR optimizer challenger that proves net utility
→ explicit close-execution / actual-fill contract
→ CIQSEC↔broker instrument/account map + signed intent
→ broker lifecycle/open-orders → canonical live book
→ broker restart reconciliation / persistent FREEZE_NEW_RISK
→ research-vs-broker IS/fees/timing/cash-drag bridge
→ long-side IS/capacity + independent replication
→ BOUNDED LONG/CASH CAPITAL
→ only if economically attractive: PIT borrow → true L/S Shadow B → bounded L/S capital
```

As soon as the first evidence-backed mutation exists, establish only the minimum research-capital roles:

```text
Safety Parent
Incumbent
Challenger
Negative Control
```

Additional Challengers and Sentinel are evidence-triggered expansions, not prerequisites. Full promotion-state machinery, search-debt automation, hidden-OOS platform, AOV-2, provider breadth, and sentinel governance are not prerequisites for the first seal or first bounded AI development mutation.

## Forbidden scope

Before Clock #1: no Episode-3 product milestone; no universal PIT platform; no second engine/app; no compatibility restoration; no ticker fallback; no unrestricted generated Python; no optimizer/RL-first route; no broad provider acquisition; no AOV-2/event/pattern authority; no broker/order execution work; no outcome opening; no alpha claim; no live capital.

After Clock #1, the only broker-related widening approved by this roadmap is the independently owned thin PAPER Capitalization Vertical. It reuses the existing broker/recovery/signed-replay/event-book substrate; it does not authorize a second OMS or strategy live capital.

## Validation snapshot

- Gate A exact commit `39f7be3`: `115/115` selected tests PASS from `git archive` bytes.
- Gate B hard-cut tree: ZERO-COMPAT all seven counters `0`.
- AOV suite: destructive v3 temporal/adversarial authority `75/75 PASS`; ZERO-COMPAT seven-zero; active runtime grep finds no v2/open authority reference outside explicit negative tests.
- Hardened research selected suite: `33/33` PASS.
- Canonical dashboard/book/historical-receipt suite: `33/33` PASS.
- Hard-cut Episode-2 regression: `107/107` PASS.
- Historical Alpha ship-runtime substrate in live Git checkout: `7/7` PASS.
- Python compilation, YAML parse, `pip check`, and `git diff --check`: PASS.

Portfolio-alpha evidence remains exactly `0`.

## What Was Done

- Froze Episode-2 locally at exact `39f7be3894623c095994066b8f0ea2895b968643` and proved the exact archived-byte selected matrix `115/115`.
- Performed the destructive AOV hard cut: removed duplicate root app/launcher authority, legacy book projection, current-root Alpha rebuild compatibility, ticker/asset aliases, transitional authority fallbacks, mutable evidence-manifest bypass, and unnamed benchmark selection.
- Hardened the canonical research spine and implemented the minimal AOV cube, Rule100 control, deterministic Parent, one risk-reducing Child, hash DAG, five-arm experiment/seal machinery, and official-SOFR economic cash.
- Built the deterministic Parent–Child review core with reconciliation, CVaR helped/hurt, cost-dominated/insufficient/accounting statuses, packet hashing, and single-episode no-mutation authority.
- Froze insurance V0 at materiality `0.05` and annual premium ceiling `0.0015`; any change creates a new contract/model family.
- Corrected the equity-authority premise: the available WRDS account lacks CRSP entitlement. Owner explicitly authorized Path B, so active AOV was destructively recut to S&P Capital IQ Pro with canonical `CIQSEC:<Capital IQ Security ID>` identity and CIQ primary-security total-return authority; no PERMNO/ticker/company-entity compatibility path remains.
- Froze `run_4.xlsx` as the single 109-company universe + bounded current-cut company-fundamental source. Its receipt carries both `COMPANY_UNIVERSE` and `QUARTERLY_FUNDAMENTALS`; `run_2.xlsx` is historical evidence only. `run_4` materializes 1,203 absolute-quarter rows plus 109 current fundamental states with canonical Rule100 V1.1 `factor_positive_count`; 56 are complete, 52 partial, one has no absolute-quarter history. The artifact is conservative admission-time PIT only and keeps `SP_ENTITY_ID` temporary.
- Implemented the bounded current-cut CIQ Security/market admission tail: unique primary `CIQSEC:` + Trading Item identity, mechanical exclusion instead of compatibility fallback, 200-day market warmup, current Rule100 v1 `0.35` sizing, market/AOV primitive construction, atomic raw/output receipts, hard-gated direct NY Fed SOFR intake, and exact decision-cut construction. Historical market warmup rows carry neutral Q/max uncertainty/no factor counts; only the target date receives the current `run_4` factor authority. Same-day U.S. daily market retrieval before 16:00 America/New_York and future-stamped source retrieval times block before output, and the cut builder independently rechecks the 16:00 rule.
- Proved the one-row current-cut design through the destructive v3 candidate/promotion path synthetically; AOV is `75/75 PASS`, ZERO-COMPAT remains all seven zero, target-date primitive returns reconcile exactly to P&L, cut/seal/evaluation times are separated and bound to next NYSE close, executable bytes are hash-bound, fresh-process proof is mandatory, and the separate receipt alone starts clock authority. The real first-seal path remains blocked because the real CIQ primary-security mapping and completed market raw objects are not yet admitted.
- Proved CIQ provider capability through the installed authenticated Office client without changing AOV authority: direct Genix `SPGLabel` transport returned a valid provider response; exact-date `SPGTable` returned `SP_TOTAL_RETURN`, `SP_PRICE_CLOSE`, and `SP_VOLUME`; and `SPG(..., as_of_date, ...)` returned historically changing PIT fundamentals. The historical capability is explicitly post-Clock only.
- Solved primary identity and captured the frozen 109-name raw master. Identifier Lookup + scalar SPG proved `SP_CIQ_ID` is security-level and `SP_TRADING_ITEM_ID` listing-level; direct company-key `SPGTable` returned the current primary pair for all 109 names. Raw master: `data/aov0/raw/ciq_primary_security_master_20260808T162322Z.csv`, retrieval `2026-08-08T16:23:22.0736860Z`, SHA-256 `8aefbbd751a714b8689402ccbf8fa2b776c6388d4bbc3870ec4f8b975306eca4`. Market chunk testing proved exact-primary-SPT 7-day chunks are the largest stable unit; nine prior 5-day parts and the validated target-week 7-day SPT part are in custody. Detailed evidence: `docs/context/ciq_provider_acquisition_findings_20260808.md`.
- Admitted direct NY Fed SOFR after the real 15:00 ET gate at `2026-08-07T19:00:08.894288Z` with raw SHA-256 `445ca1ae93a7ae904681716d8e37088fab905ae4f74f32fc2619a459918d54cc` and output SHA-256 `ed75219416a524f17cb3e29b9e4fadff2dcfa1d12a8368d752007aac779c4e5e`. No real CIQ security-master/market raw-object admission, real seal, push, hosted CI execution, publication, outcome opening, or live-capital action occurred.

## What Is Locked

- Architecture/scientific-bar authority remains approved and must not be reopened. Prior strategic and sim-to-real recuts are retained; latest alignment=`APPROVED_WITH_WINNER_AND_AUTHORITY_RECUT` at ~99/100 after recuts and top-level design is effectively closed. The active execution milestone remains `PRE_SEAL_REAL_CIQ_ADMISSION` → first verified Clock-Start Receipt. `CYCLE_RESONANCE_v1` is preregistered only; no Atlas/AI/provider/broker implementation is authorized before Clock #1. Post-Clock Right-Tail Atlas forensics are discovery-only: use qualifying episodes rather than famous companies, retain false-winner/missed-winner/matched controls, audit incumbent winner-blindness without tuning, and keep entry/discovery versus continuation/hold versus exit/falsifier skill distinct.
- System-wide Destructive Authority Replacement is frozen: when future current authority changes, remove the old active writer/reader/fallback/alias/dual-write/feature flag/compatibility adapter in the same slice; keep old artifacts as immutable historical evidence only. Owner governance is six constitutions: Fund Mandate, Research, Capital, Execution, Live-Risk, Operator.
- `launch.py` → `dashboard.py` is the sole current application and `core.engine.run_simulation` through the hardened `research/` runner is the sole simulation authority.
- Gate-A local candidate is `39f7be3`; Gate-B local executable lineage ends at `dca69fc` before documentation closure.
- Active AOV equity identity/source family is S&P Capital IQ Pro only: one hash-bound `run_4` universe/fundamentals object + `CIQSEC:<id>` security master + primary-security total returns. `run_2` and historical CRSP/PERMNO material remain audit history only.
- Economic cash is official SOFR minus 25 bp, ACT/360, no zero floor, post-publication only, with no proxy substitution.
- Production insurance V0 is frozen at `0.05 / 0.0015`; it may not be calibrated in place from subsequent results.
- Portfolio-alpha evidence remains `0`; the prospective clock has not started; Limited Live remains closed.
- Current-cut factor authority exists only on the target date. Historical market rows are rolling-state warmup with neutral Q/max uncertainty/no factor counts; they cannot become historical Rule100 factor authority.

## What Is Next

- Destructive v3 temporal authority and the mandatory adversarial failure-injection suite are closed locally; do not reopen them or add cleanup refactors before Clock #1.
- When acquisition resumes, verify/reuse the captured 109-name primary master; identity discovery/UI traversal is no longer the active task.
- Complete exact-primary-SPT market history in 7-weekday atomic chunks using `SP_TOTAL_RETURN`, `SP_PRICE_CLOSE`, and `SP_VOLUME`; combine/dedupe/hash the raw market object and verify >=200 actual completed closes per admissible name through `2026-08-07`. Preserve existing raw parts and do not backfill short histories from alternate listings.
- Restore the repo-approved `.venv` interpreter before Python admission; the `.venv` directory exists, but `.venv/Scripts/python.exe` is absent at handover. Then run the existing fail-closed builder, build `decision_cut_v3`, and write the real Seal Candidate → fresh-process verification proof → immutable Clock-Start Receipt. Only then does the prospective clock start.
- Weekly operation after Clock #1: freeze original 109 candidate entities; refresh fundamentals, primary-security/status, completed market and rate/benchmark measurements each cut; fail stale REQUIRED data closed; do not rerun the growth screen.
- WIP law after Clock #1: weekly tape + review/custody stay active; exactly one Alpha **build** lane at a time, first=`CYCLE_RESONANCE_v1`, while already-sealed Challengers continue as immutable running tapes. The first family is Minimum-Viable-Atlas-first, not full-history-first: enough legitimate PIT history for honest discovery/controls → freeze → untouched test where available → seal prospectively; then deepen history/controls/replication underneath the running clock. Deeper right-tail work is forensic discovery only: enumerate qualifying episodes under the frozen outcome; retain false-winner, missed-right-tail and matched ordinary/left-tail contrasts; audit Rule100/Parent/Child winner-blindness without tuning; separate discovery/entry from continuation/hold and exit/falsifier alpha. Independent-replication prep and borrow entitlement remain async. Independent Ops/Engineering starts the thin Alpaca PAPER Capitalization Vertical immediately with `MOC_CLOSE_AUCTION_V1`, one promoted policy→one rebalance identity, account/instrument map, full lifecycle/open-orders, restart fencing/FREEZE_NEW_RISK, and research↔broker IS attribution while `financial_alpha_evidence=0`. Optimizer remains challenger; no generic Right-Tail/AI/data platform.
- After a matured validated ReviewPacket, allow outcome-informed MutationManifest; independently pre-registered Challenger builds need not wait for maturity. Before bounded long/cash capital require prospective evidence + one genuinely independent replication + Capitalization Vertical authority + owner/risk approval; true L/S is a separate optional extension. Optional micro-live canary is an explicit owner/risk exception only and remains `financial_alpha_evidence=0`.
- At major matured evidence checkpoints, PM/CEO must classify the Alpha Program `CONTINUE`, `PIVOT`, or `STOP / HOLD`; absent alpha does not automatically authorize more architecture/data/optimizer/L-S complexity.

## First Command

```text
NEXT ACTIVE ACTION WHEN RESUMED: reuse/hash-check the captured 109-name primary master; complete remaining exact-primary-SPT market history in 7-weekday atomic chunks until >=200 actual completed closes per admissible name through 2026-08-07; combine/dedupe/hash raw market custody; restore/use repo .venv; then run fail-closed CIQ builder → decision_cut_v3 → Seal Candidate → fresh-process verification → Clock-Start Receipt. Do not redo identity discovery, retry SNLQuery/SNLPrice/SPGRANGE, execute v2/open authority, or tune Parent/Child.
```
