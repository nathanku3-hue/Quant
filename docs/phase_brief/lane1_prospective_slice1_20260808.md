# Lane 1 — Prospective Slice 1

**Mode:** EXECUTION_PACKET
**Date:** 2026-08-08
**Authority:** APPROVE NEXT PHASE
**Clock #1:** RUNNING
**Financial alpha evidence:** `0`

## Hierarchy

- L1: Terminal Zero quantitative research console / AOV post-Clock programme.
- L2 active stream: Lane 1 Prospective / Self-Improvement (Data + Research Engineering + Docs/Ops).
- L2 deferred from this worker: Lane 2 historical A1→A2, PAPER Capitalization, Market Transition confirmatory authority, repository-close repair.
- L3 stage: Executing → Iterate Loop for weekly-tape preflight + Alpha PIT v1 concrete producer mechanics + CRV1 input-packet/implementation-manifest mechanics; the independent broad CRV1 risk-set capture, remaining source surfaces, empirical candidate, and recurring weekly seals remain next.

## Product result

Keep prospective evidence time alive independently of post-Clock engineering, while advancing the narrow Alpha PIT v1 → CRV1 seam from fixtures into fail-closed real-current-custody mechanics without opening outcomes, borrowing the AOV growth-screen denominator, or changing Parent/Child.

## Journey state

```text
Clock #1 running
├─ weekly frozen-109 tape (independent, always on)
└─ Alpha PIT v1 → CRV1 deterministic fixtures → later real PIT join
```

Outcome-informed mutation remains unavailable until a matured, reconciled, validated ReviewPacket exists.

## Do now

1. Prove each weekly attempt uses the exact original 109 candidate entities and fresh required receipts for fundamentals, primary-security/status, completed market data, and SOFR before v3 cut construction.
2. Expose a provider-blind `alpha_pit_data_api_v1` read capability with permanent `CIQSEC:` identity, `available_at` enforcement, content hashes, receipt bindings, coverage/missingness, and no outcome capability on CONFIRMATORY/PROSPECTIVE objects.
3. Provide deterministic zero-authority fixtures for engineering.
4. Make `CYCLE_RESONANCE_v1` consume only the Alpha PIT read capability and close the four input artifact hashes plus source-receipt hashes into one deterministic input packet.
5. Implement concrete CIQ structured + SEC claims adapter boundaries, preserving unlanded fields/sources as explicit missingness and refusing to backdate current-cut bytes.
6. Require an independent non-growth CRV1 risk-set source with a frozen eligibility-contract hash and independent identity receipt; never relabel the AOV 109 growth-screen laboratory.
7. Implement a CRV1 implementation-manifest freeze gate with no scientific defaults and explicit search-budget/code-byte custody.

## Done when

- frozen-109 membership drift fails closed;
- stale/missing/future required weekly receipts fail closed;
- weekly preflight grants no seal/outcome/mutation/alpha authority;
- CONFIRMATORY/PROSPECTIVE capability objects have no `outcomes` method;
- ticker/legacy identity and `available_at > as_of` fail closed;
- repeated fixture requests hash identically;
- CRV1 package has no provider or discovery-outcome import;
- fixture CRV1 packet carries zero financial-alpha authority;
- current CIQ structured custody validates mechanically with explicit per-field missingness and no historical backdating;
- CRV1 risk-set source fails closed without the independent base-rate/eligibility/identity contract;
- every scientific implementation-manifest parameter is explicit and hash-bound; missing parameter/search-budget overrun/tamper fails closed;
- Lane-1 focused tests and AOV regression pass;
- ZERO-COMPAT remains seven zeros through its contract test.

## Stop only if

- Clock #1 custody hashes or immutable receipt no longer verify;
- frozen 109 membership cannot be recovered exactly;
- required weekly data is stale/missing;
- a change would require outcome opening before `2026-09-09T20:00:00Z`;
- a change would retune Parent/Child or grant strategy capital authority;
- a same-domain writer collision cannot be reconciled without reverting unrelated work.

## Forbidden scope

No growth-screen rerun; no Parent/Child retuning; no outcome-informed CRV1 mutation; no historical A1/A2 evidence claim; no leverage, shorting, options, Limited Live, or strategy live capital; no repair of the nine repository-close collection errors on this critical path; no push/publication.

## Validation evidence

- Alpha-PIT + CRV1 focused: `19/19 PASS`.
- AOV regression: `102/102 PASS`.
- ZERO-COMPAT contract test: PASS, asserting all seven counters equal zero.
- Current real CIQ custody diagnostic: 109 identities; SMA200 present `104/109`; gross-margin and cash-from-operations `109/109 MISSING_SOURCE`; expectations `981/981 MISSING_SOURCE`; SEC claims source unlanded; CRV1 risk-set join blocked because the independent non-growth base-rate source is not landed.
- `research/alpha_pit_v1`, `research/cycle_resonance_v1`, `research/aov0/weekly_tape.py`, and `scripts/alpha_pit_validate_current_ciq_custody.py`: compile PASS.
- `git diff --check`: PASS.
- No separate ZERO-COMPAT CLI result is claimed.
