# AO-K0 — Asymmetric Opportunity Constitution + Minimal Basis Freeze

**Date:** 2026-08-11  
**Status:** `CLOSED_DOCS_FREEZE / NO_EMPIRICAL_READ`  
**Constitution:** `docs/architecture/asymmetric_opportunity_constitution_v1.md`  
**Machine freeze:** `docs/architecture/ao_k0_minimal_basis_freeze_v1.json`  
**financial_alpha_evidence:** `0`  
**W6:** `UNTOUCHED`

## Objective

Replace the universal PREBREAKOUT scientific unit with `OpportunityKernelV1`: the smallest PIT representation that predicts one explicit future state/payoff distribution and is evaluated jointly for edge, asymmetry, and capturability.

This is a local scientific-unit recut, not a broad architecture reopen. Existing strategic direction, independent evidence clocks, PIT custody, search debt, one capital-policy authority, and historical frozen contracts remain intact.

## Frozen output

AO-K0 freezes:

- `OpportunityKernelV1` required fields and kernel taxonomy;
- `CONTINUATION` as first entry incumbent and `DISLOCATION` as deferred/higher-burden entry mode;
- `AsymmetryEvaluationV1` with right-tail and catastrophic-left-tail reporting together;
- existing `ECONPHYSICS_PREBREAKOUT_v1` as a specialized `FUNDAMENTAL_TRANSITION / MECHANISTIC` kernel rather than a universal stock ontology;
- first fixed basis package `AO_K0_BASIS_Q_M_QM_V1` with `Q`, `M`, and `Q×M` arms;
- broad date-local `AO_BASIS_US_PRIMARY_COMMON_DATE_LOCAL_V1` denominator law;
- weekly decision schedule, 20-session forward target, `K_t=ceil(5%×|D_t|)`, canonical one-bar execution, and 10 bp turnover cost;
- zero charge for this freeze and exactly one material trial for the first joint result-bearing Q/M/Q+M development read.

## Exact first basis

```text
Q:
  four fundamental groups only
  = z_demand
  + z_inventory_quality_proxy
  + z_moat
  + capital_cycle_score
  via date-local percentile ranks
  require >=3 observed groups
  q_score = mean(observed group ranks)
  no missing fill

M:
  m_raw = trailing 60-observed-session primary-security total return
  m_score = date-local percentile rank(m_raw)
  no residualization / sector neutralization / vol normalization / breakout gate

Q+M:
  qm_score = q_score * m_score
  no learned weights or threshold search
```

Each arm must fill the same frozen `K_t`. If observability is below K, the date is a coverage failure; K and the denominator do not shrink.

## Current evidence rerouting

- Inventory mean reversion remains a diagnostic future `FundamentalTransitionKernel` challenger and is not the first universal basis.
- Margin remains a candidate, not a hard survivor.
- Revenue is parked.
- Rule100-style fundamental state is a quality/compounding representation/control, not automatically proven Alpha.
- Momentum may later be standalone continuation under its own identity; `VSB_CONFIRMATION_v1` remains downstream confirmation only.
- Dislocation is deferred until a quality kernel and explicit falsifier/downside floor exist.

## Hard stop-lines honored

This slice performs no provider capture, W6 read, winner-label read, Q/M/Q+M empirical run, new inventory/margin transform, revenue rescue, expectation-gap build, dislocation backtest, ticker-specific rule, generic agent platform, optimizer, Parent/Child mutation, A2 re-query, Clock #1 outcome open, broker action, or capital promotion.

## Exit

AO-K0 is closed at the constitution/freeze layer. The next result-bearing action is **not authorized by this brief alone**. Once exact lawful broad development PIT source bytes are admitted and uninspected outcome labels are hash-bound, the first Q/M/Q+M development run charges one material basis trial before result inspection. Only a frozen survivor may later approach W6 once.