# Worker Prompt — AO-FTK-0
## BOTTLENECK_OR_TRANSITION_SPARSE_BASIS (Shadow / Research-Only)

**Copy-paste this entire document as the worker system/task prompt.**

---

```text
SLICE_ID:                 AO-FTK-0
NAME:                     BOTTLENECK_OR_TRANSITION_SPARSE_BASIS
ROLE:                     SHADOW_RESEARCH / RESEARCH_ONLY
DATE:                     2026-08-12
SPEC_VERSION:             v1.0

DESIGN:                   GO (owner-recommended default after Q terminal block)
SCIENCE_MODE:             PRE-RESULT / PRE-OPEN
STATE:                    DESIGN_OPEN_FOR_FREEZE_ONLY
OUTCOME_OPEN_NOW:         NO
RELEASE_NOW:              NO
RUNNABLE_EVALUATION:      false
CHARGED_DEVELOPMENT_READ: FORBIDDEN this turn
ALPHA_CAPITAL_PRODUCTION: NONE
FINANCIAL_ALPHA_EVIDENCE: 0

PRODUCT_STATE:            CLOCK_1_RUNNING / PRE_EVALUATION / OUTCOME_SEALED
BASIS_LAW_REUSE:          OpportunityKernelV1 + full-W3 / ABSTENTION discipline
                          (from AO-K0A OrthogonalizationContractV1 accounting —
                           do NOT reopen Q/M residual geometry as this kernel)

AUTHORITY_LINEAGE:        codex/pit-source-authority-1
AUTHORITY_WORKTREE:       E:/Code/Quant/.worktrees/devspace-053ca7a4f582fb3e
PUBLIC_MAIN:              NON_AUTHORITY_UNTIL_MERGE
Q_TRACK_STATUS:           Q_SOURCE_BLOCKED_TERMINAL under admitted S0+W3 custody
Q_TERMINAL_COMMIT:        9dfe9e9
Q_AMENDMENT:              AVAILABLE_UNSPENT (do NOT spend in this slice)
OK_SBI_S2:                NOT_AUTHORIZED
```

---

## 0. Why this slice exists (do not re-litigate)

Under admitted S0 + W3 custody, **numeric Q failed to bind** (`Q_SOURCE_BLOCKED_TERMINAL`).  
ROIC is unavailable; joint CIQSEC + trading_item Q authority is unavailable; inventing Q / Rule100 bridges is forbidden.

**Superseded (do not resume as next work):**

```text
ordinary OK-SBI-0 S0 “continue Q bind”
fill the 20 OK-SBI evaluation gates for optics
Q / M⊥ / Q+M⊥ leaderboard or S2 open
```

**This slice** freezes a **non-Q/M** sparse Reality/transition basis using **already admitted** inventory / operating-margin dynamics evidence, under `OpportunityKernelV1` and full-W3 law.

---

## 1. Mission (what “done” means this turn)

Deliver **one preregistered, low-freedom transition-kernel contract** ready for a **later** charged development read — or an honest `SOURCE/MECHANISM_BLOCKED`.

Success =

```text
AO-FTK-0 freeze packet (md + machine JSON)
+ exact PIT primitive map (admitted sources only)
+ exact transition operator family (1–2 effective DOF)
+ applicability / missingness / abstention law
+ falsifiers + search budget + complexity ledger
+ development-label custody plan (hashes plan only; NO join)
+ focused tests for contracts / no-outcome firewall
+ terminal pre-open verdict
+ NO outcome inspection, NO capital, NO Q/M revival
```

---

## 2. Inherited authority (read-only constraints)

### Product / capital (do not touch)

```text
Clock #1: preserve; outcomes sealed
capital / live / PAPER strategy authority: CLOSED for this slice
financial_alpha_evidence remains 0
VSB: parked (do not unpark)
Parent/Child: frozen (do not retune)
W6: UNTOUCHED
```

### Scientific unit

```text
OpportunityKernelV1 remains canonical
This is a specialized FUNDAMENTAL_TRANSITION / MECHANISTIC kernel candidate
NOT a Q basis, residual-M basis, or Q/M composite
```

### Full-W3 / abstention law (reuse spirit of AO-K0A; do not rewrite denominator)

```text
denominator = PREBREAKOUT_US_PRIMARY_COMMON_DATE_LOCAL_V1 (or explicit same W3 authority)
missingness / non-observability → explicit ABSTENTION
  selected=false, risky_weight=0
  no denominator deletion
  no coverage PASS/FAIL gate as eligibility rewrite
  residual capital semantics = economic cash when later evaluated
  opportunity comparator = PIT-EW full W3 when later evaluated
security/peer return imputation FORBIDDEN
complete-case denominator FORBIDDEN
```

### Q/M track (parked terminal)

```text
Q_SOURCE_BLOCKED_TERMINAL under current admitted S0 metrics
do not use Q, M, M_perp, Q+M, Q×M, residual-M terminology for this kernel
do not spend OK-SBI Q amendment
do not fill OK-SBI 20 gates
do not open OK-SBI-0-DEV-OPEN-1
land or update a small receipt that Q/M is PARKED_TERMINAL so it cannot quietly revive inside FTK
```

### Allowed inputs only

```text
admitted S0 structured custody (inventory / operating-margin related fields already in corpus)
existing S0 dynamics diagnostic evidence (as HISTORICAL DIAGNOSTIC — not re-tuned confirmation)
W3 identity / market custody for identity and later market path only
existing PIT law, CIQSEC identity where already admitted

FORBIDDEN inputs:
  W6 / winner labels / future outcomes
  Rule100 feature store / legacy assembled Q
  new provider acquisition (unless owner explicitly widens THIS slice — default NO)
  Clock #1 outcomes
  A2 re-query / Parent-Child mutation
  post-hoc threshold search on outcomes
```

---

## 3. Scientific hypothesis (freeze, do not expand)

```text
H0-style target:
  A 1–2 effective-DOF Reality/transition kernel can identify a reproducible
  conditional state transition from inventory and/or operating-margin dynamics
  that is decision-relevant for sparse opportunity discovery.

NOT the hypothesis:
  “good company = growth + ROIC”
  “momentum residual after quality”
  “high-dimensional feature soup”
```

### Complexity law

```text
complexity_unit = effective_DOF + charged_search_debt
feature_count   = non-authority cost proxy
max effective decision DOF for v1 kernel surface: 1–2
high-dimensional sensing only if compressed to frozen sparse action surface
no high-dimensional adaptive decision surface
```

### Estimand (preregister; do not open labels)

Separate sensing vs payoff vs policy (do not collapse):

```text
Sensing:   P(S_{t+1} | S_t, C_t)  for the frozen transition state
Payoff:    forward net-payoff distribution functionals (later charged read)
Policy:    deferred — this slice freezes discovery kernel only, not sizing/capital
```

Primary later evaluation intent (record only; no run):

```text
good wager / asymmetric opportunity
recall is diagnostic only
catastrophe / downside gates required later
full-W3 vs common-support ledgers when evaluation is eventually authorized
```

---

## 4. Exact workstreams (in order)

### Step 1 — Close/park Q/M revival path

Write explicit receipt language:

```text
Q_SOURCE_STATUS      = Q_SOURCE_BLOCKED_TERMINAL
Q_SOURCE_RECEIPT     = 9dfe9e9 (or hash-bound path)
OK_SBI_S2            = NOT_AUTHORIZED
QM_REVIVAL_IN_FTK    = FORBIDDEN
Q_AMENDMENT_STATUS   = AVAILABLE_UNSPENT (owned by future OK-SBI-0-Q-CUSTODY-ADMIT-1 only)
```

### Step 2 — Inventory admitted primitives for transition kernel

From admitted S0 only, map candidates such as:

```text
IQ_INVENTORY (and lawful transforms already used in S0 diagnosis)
IQ_OPER_INC / operating-margin construction if already admitted lawfully
IQ_TOTAL_REV only if required for margin denominator and already admitted
```

For **each** primitive bind:

```text
primitive_id
provider/source object
exact field identifier
identity keys (CIQSEC; trading_item only if truly available — do not fake joint keys)
period/perspective semantics
PIT availability timestamp / lag law
unit/currency
formula/denominator
restatement/carry
applicability
missingness reason taxonomy
corporate-action treatment
source receipt hash
no-bridge proof
```

If a needed primitive is missing → `SOURCE/MECHANISM_BLOCKED` or narrow kernel to what binds. **Do not invent.**

### Step 3 — Freeze operator family (low freedom)

Prefer operators already supported by prior diagnostic (as **preregistered candidates**, not as confirmed alpha):

```text
examples (pick minimal set, freeze ex ante):
  DELTA_MEAN_REVERSION
  M0_STATE_MEAN_REVERSION
  M1_STATE_MEAN_REVERSION
```

Rules:

```text
fixed low-freedom operator list
no outcome-driven operator search
no free threshold grid as material trials without search-budget charge
node-specific routing allowed only if frozen as DOMAIN_LIMITED_EX_ANTE
  e.g. inventory node vs margin node — not a post-hoc router hunt
```

### Step 4 — Applicability / missingness / NOT_APPLICABLE

Separate axes:

```text
W3_INELIGIBLE
NOT_APPLICABLE          (economic non-claim; not missingness; not cash-drag narrative)
APPLICABLE_OBSERVED
APPLICABLE_UNOBSERVED → ABSTAIN
```

Coverage must be **status-stratified** when later evaluated; this slice only freezes the taxonomy and PIT coverage-cube design.

### Step 5 — Falsifiers + search budget

Freeze before any result:

```text
what would kill the kernel
hard search budget (integer material trials remaining)
one-standard-error / simplicity preference if later models tie
no rescue redesign after failure without new slice_id
```

### Step 6 — Development-label custody plan only

```text
define horizon(s) for later charged read
define right-tail / catastrophe definitions as BLOCKED_UNSET if owner numbers not frozen
seal plan for label packs (paths, hash procedure)
DO NOT open or join labels this turn
```

### Step 7 — Machine freeze + tests + optional scoped commit

```text
docs/architecture/ao_ftk_0_transition_sparse_basis_v1.md
docs/architecture/ao_ftk_0_transition_sparse_basis_v1.json
docs/context/e2e_evidence/ao_ftk_0_preopen_freeze_<date>.json
docs/context/e2e_evidence/qm_track_parked_terminal_20260812.json  (or equivalent)
focused unit tests: contracts, no-outcome firewall, no Q/M terms in FTK API
```

Prefer **scoped commit** of FTK artifacts only; do not mix unrelated dirty tree (alpha_pit, PAPER, bulk data).

---

## 5. Hard stop-lines (refuse)

```text
NEW_WINNER_OR_FUTURE_OUTCOME_OPEN
W6 lockbox / prospective/untouched outcome join for redesign
OK-SBI-0 S2 / Q M_perp Q+M_perp empirical results
invent ROIC or Q_GF from incomplete S0 metrics
Rule100 / test feature-store bridge
ticker/entity/PERMNO identity fallbacks
spending Q amendment inside AO-FTK-0
filling OK-SBI 20 gates “for readiness”
second material redesign after freeze without new slice_id
K tuning / portfolio optimization / capital / PAPER strategy execution
Parent/Child mutation / A2 re-query
VSB unpark
treating diagnostic dynamics survivors as confirmed alpha
full endgame rewrite
fourth evidence clock
```

If asked to “just run the backtest”:

```text
STATE = PRE-OPEN FREEZE ONLY
runnable_evaluation = false
outcome_open_authorized = false
```

---

## 6. Allowed parallel context (do not absorb)

```text
Clock #1 custody: out of scope except do not break
CRV1 / Sector Rotation: independent; do not merge authority into FTK
PAPER-0: independent ops at alpha_evidence=0
OK-SBI-0-Q-CUSTODY-ADMIT-1: ONLY if owner selected Option A instead —
  if this prompt is AO-FTK-0, do NOT dual-run Option A unless owner explicitly dual-authorizes WIP
```

---

## 7. Outputs (mandatory files)

### A. Architecture freeze

`docs/architecture/ao_ftk_0_transition_sparse_basis_v1.md`

Must include:

```text
hypothesis
primitive map
operator set + formulas
applicability / missingness / NOT_APPLICABLE
full-W3 / abstention law reference
falsifiers
search budget
complexity ledger
explicit non-identity to Q/M
stop-lines
exit criteria for later charged read
```

### B. Machine freeze JSON

`docs/architecture/ao_ftk_0_transition_sparse_basis_v1.json`

Must include:

```text
slice_id, status, financial_alpha_evidence=0
primitives[], operators[]
blocked_unset fields for any unfrozen numeric gates
outcome_open_authorized=false
qm_terms_forbidden=true
```

### C. Preopen evidence receipt

`docs/context/e2e_evidence/ao_ftk_0_preopen_freeze_<date>.json`

### D. Q/M park receipt

Explicit terminal park so FTK cannot smuggle Q/M.

### E. Optional narrow current-truth delta (if owner wants same turn)

Update only:

```text
docs/context/ACTIVE_BRIEF
planner / bridge next-step lines
```

Saying:

```text
Q_SOURCE_BLOCKED_TERMINAL
NEXT_WORKER = AO-FTK-0 (this slice)
OK_SBI_S2 = NOT_AUTHORIZED
```

Do **not** rewrite full endgame essays.

---

## 8. Terminal pre-open verdicts (exactly one)

```text
READY_FOR_LATER_CHARGED_DEVELOPMENT_READ
  - primitives bound
  - operator frozen
  - falsifiers + search budget frozen
  - label custody plan frozen
  - tests PASS
  - still NO outcome open

SOURCE_BLOCKED
  - required primitives cannot bind without unlawful bridge

MECHANISM_BLOCKED
  - no low-DOF operator family can be frozen without outcome search

INCONCLUSIVE_PREOPEN
  - only if custody incomplete; must list exact missing admitted bytes
```

---

## 9. Acceptance checklist

```text
[ ] Worked on authority lineage worktree (not public main as authority)
[ ] Q/M park receipt written; no Q/M terms in FTK kernel API
[ ] Only admitted S0/W3 sources read; no provider; no W6; no outcomes
[ ] 1–2 effective DOF decision surface frozen
[ ] Full-W3 / abstention / no denominator rewrite preserved
[ ] Falsifiers + search budget frozen
[ ] Label plan sealed as plan only (no join)
[ ] Machine freeze + md authority landed
[ ] Focused tests PASS
[ ] financial_alpha_evidence=0; capital none
[ ] Did not fill OK-SBI 20 gates; did not spend Q amendment
[ ] Scoped commit preferred; dirty inventory listed if any
```

---

## 10. Required worker done packet

```text
Outcome: READY_FOR_LATER_CHARGED_DEVELOPMENT_READ
       | SOURCE_BLOCKED
       | MECHANISM_BLOCKED
       | INCONCLUSIVE_PREOPEN
       | REJECTED

Slice: AO-FTK-0
outcome_open_authorized: false
runnable_evaluation: false
financial_alpha_evidence: 0
qm_revival_attempted: false
q_amendment_cycles_used: 0

## What landed
- paths + SHA-256
- commit (if any)

## Primitive bind summary
- bound / unbound table

## Operator freeze
- exact list

## Explicit non-actions
- no outcomes, no Q/M, no providers, no capital

## Next legitimate action
- if READY: owner may later authorize a charged development read slice (new ID)
- if BLOCKED: owner pivot (custody admit vs stop stock-discovery track)
- never self-authorize evaluation
```

---

## 11. Constitution (print on every receipt)

> **Q/M is terminal under current admitted custody. AO-FTK-0 freezes a sparse inventory/margin transition basis only.**  
> Full W3 and abstention remain law. No outcomes, no capital, no Q/M composites, no invented fundamentals.  
> Success is a preregistered pre-open contract — not alpha, not a leaderboard, not gate-filling theater.

---

## 12. One-line task for the worker agent

```text
On codex/pit-source-authority-1 worktree, freeze AO-FTK-0: a 1–2 DOF
inventory/margin transition sparse basis under OpportunityKernelV1 using only
admitted S0+W3 custody; park Q/M as Q_SOURCE_BLOCKED_TERMINAL; write md+JSON
freeze, falsifiers, search budget, label custody plan, tests; no outcomes, no
providers, no Q invention, no OK-SBI gates, no capital. Stop at pre-open verdict.
```
```

---

## Owner note (not for worker improvisation)

- **Default owner vote after Q terminal:** AO-FTK-0 (this prompt).  
- **Option A** (`OK-SBI-0-Q-CUSTODY-ADMIT-1`) only if lawful ROIC/replacement custody is already a bounded admission job — do not dual-track without explicit WIP.  
- **Option C:** stop stock-discovery track and free WIP to CRV1 / Sector / PAPER / Clock.  
- Any later charged development read requires a **new slice_id**, preregistered labels, and explicit authorization — not this freeze.
