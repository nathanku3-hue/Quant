# Phase Brief — Result-First AI Research Loop Method Lock

**Date:** 2026-08-12
**Mode:** `APPROVAL_GATE / DOCS_PROTOCOL_SYNC`
**Owner decision:** `APPROVED — LOCK HARD WORDING`
**Execution effect:** forward-only scientific/research-process authority; no family/product/capital action
**financial_alpha_evidence:** `0`

## Hierarchy

| Field | Expertise Level | Rationale |
|---|---|---|
| L1 Terminal Zero quantitative research console | Owner / system | Product and evidence authority remains unchanged |
| L2 Research Method + Docs/Ops | Architecture / Alpha governance | This round changes future scientific-process contracts only |
| L3 Result-first AI research-loop method lock | Protocol / verification | Forward gate, RDV, capturability, current-truth sync, SAW, commit |

## Decision to change

Replace phase-completion / run-completion research planning with a decision-producing, forward-gate-only method that spends engineering and untouched evidence only when the result can change a legal route.

## Hard owner lock

```text
ordinal RDV only (LOW/MED/HIGH); killer rule > score
risk margins remain a vector; capturability_state is summary only
telemetry calibration is policy-conditioned
>=2 real consumers is the reuse default; safety/authority primitives may share earlier
```

Still rejected:

```text
fills → Alpha
lambda-soft hard risk
blended expert score
E6.5 on pure sensing
RDV as Alpha evidence
outcome-trained stress_block_rate
auto DISLOCATION open
```

## Follow-on (same day) — runtime closure truth

Constitution lock is not universal runtime enforcement. See:

```text
docs/architecture/ai_native_runtime_status_v1.json
docs/architecture/ai_native_runtime_closure_audit_20260812.md
docs/phase_brief/ai_native_runtime_closure_audit_20260812.md
```

```text
AI_NATIVE_ARCHITECTURE = YES_CONSTITUTIONALLY
AI_NATIVE_RUNTIME      = PARTIAL
```

## Acceptance checks

- [x] Canonical result-first method lock exists and is referenced by scientific method, AI pipeline, endgame and kernel constitution.
- [x] Machine preflight v2 encodes forward-only gate, ordinal RDV, R9/OSB, activation/invariance/F0-F1 and no automatic L4→L5.
- [x] ExecutionFeasibilityV1 encodes action-bearing trigger, hard-risk veto, multi-margin vector, policy-conditioned telemetry and ledger separation.
- [x] Harness planner/workcell/reconciliation templates enforce decision-to-change + terminal routes and no blended hard-risk score.
- [x] Current SoT/context surfaces advertise the method revision without changing FTK/TR-v0/Clock #1/capital authority.
- [x] JSON parses; research-loop CLI self-identifies the amended method; scoped whitespace/invariant checks pass.
- [x] Required SAW evidence is published at `docs/saw_reports/saw_result_first_ai_research_loop_method_lock_20260812.md` and both closure validators return VALID.
- [x] Commit allowlist is explicit and pre-existing staged state is empty; unrelated working-tree dirt is excluded from the commit path list.

## Forward-only gate

```text
L4_EMPIRICAL_FREEZE
→ READY_FOR_L5_CANDIDATE
→ cross-family ordinal RDV allocation
→ L5 only under separate authorization

READY_BUT_NOT_PRIORITY = legal
```

Every material research/engineering slice must preregister:

```text
DECISION_TO_CHANGE
CHEAPEST_DISCRIMINATING_TEST
WHY_NO_CHEAPER_TEST_EXISTS
PASS_ROUTE
FAIL_ROUTE
UNRESOLVED_ROUTE
```

If all terminal routes are identical: `DECISION_VALUE=0 → DO_NOT_RUN`.

## Scope boundary

No outcome read, provider call, family open, FTK/TR-v0 reopen, W6 access, DISLOCATION authorization, broker action, risk-limit change, capital action, or historical evidence rewrite is authorized by this method lock.
